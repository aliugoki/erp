import { ModulePlaceholder } from '@/components/module-placeholder';
export default function Page() {
  return <ModulePlaceholder name="Human Resources" note="Employees, departments, positions and attendance — the API is live (GET /hr/employees); the screens are next." />;
}
