import { ModulePlaceholder } from '@/components/module-placeholder';
export default function Page() {
  return <ModulePlaceholder name="Finance" note="Chart of accounts, double-entry transactions and invoices — the API is live (GET /finance/invoices); the screens are next." />;
}
