# Hard-pinned for reproducibility (ADR-001). Runtime deps for the ML service skeleton (Chunk 6.1).
# Heavy model libs (Prophet/sklearn/sentence-transformers/pytesseract) are added per-feature in 6.2+.
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
# Service-to-service auth (verify the API's signed x-service-token; ADR-006).
PyJWT==2.10.1
# Structured JSON logging.
structlog==24.4.0
# OpenTelemetry — traces exported to the OTel Collector (best-effort).
opentelemetry-sdk==1.29.0
opentelemetry-exporter-otlp-proto-grpc==1.29.0
opentelemetry-instrumentation-fastapi==0.50b0
# Postgres access (read stock movements, write forecasts).
psycopg[binary]==3.2.3
# Forecasting (Chunk 6.2): ARIMA via statsmodels, with a naive fallback.
numpy==2.2.1
pandas==2.2.3
statsmodels==0.14.4
# Anomaly detection (Chunk 6.3): Isolation Forest.
scikit-learn==1.6.0
# Invoice OCR (Chunk 6.4): PDF text extraction + OCR for scanned images.
pdfplumber==0.11.4
pytesseract==0.3.13
Pillow==10.4.0
# Semantic search (Chunk 6.4): ONNX sentence embeddings (BAAI/bge-small-en-v1.5, 384-dim) — the
# self-hostable, lightweight equivalent of sentence-transformers (no torch); vectors stored in pgvector.
fastembed==0.4.2
python-multipart==0.0.20
