# Build Changelog

One line per chunk: what changed, and what the gate proved. Newest at the top.

| Chunk | Tag | What changed | What the gate proved |
|---|---|---|---|
| Reporting export engine (PDF / Excel / CSV) | _(post-9.x platform feature)_ | **Printable/exportable reports across modules.** Extended the central report builder with a shared export layer (`report-export.ts`): CSV (hand-rolled), **XLSX** (exceljs), and **PDF** (pdf-lib — open-source, WinAnsi-sanitised, paginated table). The three run endpoints (`builder/run`, `presets/:key/run`, `builder/custom/:id/run`) now accept `?format=csv|xlsx|pdf` and stream a `StreamableFile` attachment (money columns rendered minor→major); JSON stays the default. Builder run methods now return a `title` for the file name/heading. Added `finance_invoices` + `pos_sales` datasets (now 7). Web: the Reporting page gains **PDF / Excel / CSV download buttons** on any run report (preset, saved, or ad-hoc) via a new `apiDownloadBlob` helper. Added **`docs/policy-engine-scope.md`** (the next effort: per-tenant business-rule/validation policy engine). | api+web typecheck 0; `pnpm -w lint` 0 errors; **224/224** unit tests. **Live-proven**: preset exported as PDF (`%PDF`, application/pdf), XLSX (`PK`, Excel 2007+), CSV (text) — each with an `attachment; filename=…` header; new finance_invoices dataset CSV with money formatted (350000.00). |
| RBAC role card — show permission chips | _(post-9.x platform ops)_ | **Web polish (follow-up to Path 2).** The Settings → Roles card now renders a role's fine-grained `permissions` as outline chips (labelled from the permission catalog, capped at 6 + "+N more") alongside the filled capability-preset chips, with a "No grants" fallback — so a permission-only custom role no longer shows an empty card. Surfaced by a real-UI verification (headless Chrome via CDP) of the role builder. | web typecheck 0; `pnpm -w lint` 0 errors. **Verified in the running web UI**: role builder opens with capability presets + the 14-module grouped permission picker; created a role with both → persists & lists; the permission-only "Invoice Clerk" card now shows "Create / edit invoices" + "Manage chart of accounts" (was blank). |
| Path 2 RBAC — Phase D (cleanup) + ADR-010 | _(post-9.x platform ops)_ | **Path 2 complete.** Removed the now-redundant `@Roles(...)` from all 18 permission-tagged business controllers (and pruned the unused role-set consts + `Role`/`Roles` imports); `@Roles` survives only on platform/admin routes (tenants, features, users, rbac, account `adminPing`). Added **ADR-010** (permission-level RBAC — the binding decision) and updated `CLAUDE.md` with a new Never (authorize business endpoints by permission, not role). Scope doc marked complete (A–D). | api+web typecheck 0; `pnpm -w lint` 0 errors (unused-vars pruned cleanly); **224/224** unit tests. **Live-proven pure no-op**: after removal, FINANCE_MANAGER finance writes ≠403 / hr 403; permission-only "Invoice Clerk" finance/invoices ≠403 / others 403; SUPER_ADMIN platform `GET /tenants` still **200** (role-guarded), wildcard perms pass finance writes. |
| Path 2 RBAC — Phase C (flip to permission enforcement) | _(post-9.x platform ops)_ | **Permissions are now authoritative — true permission-level RBAC is live.** `@ShadowPermissions` renamed to enforcing `@Permissions` across all controllers (shadow decorator deleted); `PermissionsGuard` enforces against a new access-token **`perms` claim**; `RolesGuard` **defers** when a route carries `@Permissions` (so a permission-only custom role reaches it without a built-in role). `RbacService.effectivePermissionsForUser` resolves perms = built-in `ROLE_PERMISSIONS` ∪ each custom role's member-role perms ∪ its direct perms (Redis-cached); `roles` claim retained for platform `@Roles(SUPER_ADMIN/TENANT_ADMIN)` routes. Migration `1726400000000` adds `tenant_role.permissions`; custom-role CRUD accepts catalog-validated `permissions` (role may be capabilities-only, permissions-only, or both); web role-builder gains a grouped **permission picker**. `@Roles` lines kept (RolesGuard ignores them when a permission tag is present) — reversible; Phase D may remove. | api+web typecheck 0; `pnpm -w lint` 0 errors; **224/224** unit tests. **Live-proven**: built-in roles unchanged (no-op flip — FINANCE_MANAGER finance writes ≠403, hr 403); a **permission-only** custom role "Invoice Clerk" (`roles=[]`, `perms=[finance:invoice:write]`) POSTs finance/invoices (≠403) but is denied finance/accounts & hr (403); admin-only reports/refresh still 403 for non-admin; refresh preserves `perms`; live permission edits propagate on re-login. |
| Path 2 RBAC — Phase B COMPLETE (POS/Projects/Assets/Production/Pharmacy/Ecommerce/Subscriptions/Reporting) | _(post-9.x platform ops)_ | **Final Phase B batch — every `@Roles`-guarded endpoint in the app is now shadow-tagged; no enforcement change.** Tagged all role-gated endpoints (incl. inline `@Roles`, analytics reads, GL-config) across 8 modules. New perms: `pos:sale/report:read/glconfig:write/config:write`, `project:write`, `asset:write`, `production:write`+`glconfig:read/write`, `pharmacy:operate/config`, `ecommerce:manage/finance:write`, `subscription:write`, `report:write`. Owner grants: SALES_REP+=pos:sale,pos:report,pharmacy:operate; HR_MANAGER+=project; FINANCE_MANAGER+=asset,subscription,ecommerce:finance,pos:report,pos/production glconfig; INVENTORY_MANAGER+=production,pharmacy:operate. Admin-only tiers (`pos:config`, `pharmacy:config`, `ecommerce:manage`, `report:write`) held only via wildcard. Expectation-driven `test/phase-b-batch-matrix.spec.ts`. AI has no role-gated endpoints (nothing to tag). **Phase B is now complete across all modules.** | api+web typecheck 0; `pnpm -w lint` 0 errors; **224/224** unit tests (+15 batch matrix). **Live-proven**: owner roles pass their writes (≠403), non-owners → 403; admin-only `pos/registers` & `reports/refresh` deny capability roles (403) but allow admin (400/200); **0 `RbacShadow` divergences** across all modules. |
| Path 2 RBAC — Phase B (CRM + Sales + Helpdesk) | _(post-9.x platform ops)_ | **CRM, Sales and Help Desk fully shadow-tagged; no enforcement change.** CRM: all 19 writes (2 controllers) tagged; group grown 3 → 6 (`deal`/`contact`/`account`/`lead`/`activity`). Sales: all 6 writes tagged; new group (`quotation`/`order`). SALES_REP granted all CRM+Sales writes. Help Desk: all 13 writes tagged across **two tiers** — agent endpoints → `helpdesk:ticket:write` (SUPPORT_AGENT), admin endpoints (teams/SLA) → `helpdesk:config:write` (**admin-only**; SUPPORT_AGENT deliberately excluded). Dedicated `test/helpdesk-authz-matrix.spec.ts`; CRM+Sales added to the shared `module-authz-matrix`. | api+web typecheck 0; `pnpm -w lint` 0 errors; **209/209** unit tests (+7). **Live-proven**: SALES_REP POSTs crm/clients,/leads + sales/quotations → 201; SUPPORT_AGENT works helpdesk/tickets → 201 but is **denied** helpdesk/teams → **403** (admin → 201); **0 `RbacShadow` divergences**. |
| Path 2 RBAC — Phase B (HR + Inventory) | _(post-9.x platform ops)_ | **HR and Inventory fully shadow-tagged; no enforcement change.** HR: all 43 role-gated write endpoints across 4 controllers (`hr`, `hr-enterprise`, `hr-policy`, `hr-profile`) tagged; hr permission group grown 3 → 12 (`employee`/`department`/`org`/`attendance`/`leave`/`payroll`/`performance`/`lifecycle`/`document`/`policy`/`profile`). Inventory: all 13 write endpoints tagged; group grown 3 → 5 (`product`/`stock`/`category`/`warehouse`). `ROLE_PERMISSIONS[HR_MANAGER]`/`[INVENTORY_MANAGER]` grant their full sets (dual-run consistent with `WRITE = <owner> + admins`). New shared `test/module-authz-matrix.spec.ts` parametrises the matrix per module. | api+web typecheck 0; `pnpm -w lint` 0 errors; **202/202** unit tests (+4 module matrix). **Live-proven**: a real HR_MANAGER POSTs hr/departments,/positions,/leave-types and an INVENTORY_MANAGER POSTs inventory/categories,/warehouses → **201** each, **0 `RbacShadow` divergences**. |
| Path 2 RBAC — Phase B (Finance) | _(post-9.x platform ops)_ | **Finance fully shadow-tagged; no enforcement change.** All 26 role-gated Finance write endpoints now carry `@ShadowPermissions(...)`; the finance permission group grew from 3 → 14 perms (`account`/`voucher` write+post / `invoice` / `bill` / `payment` / `vendor` / `customer` / `period` / `reconciliation` / `costcenter` / `budget` / `currency`), and `ROLE_PERMISSIONS[FINANCE_MANAGER]` grants them all (so the dual-run is consistent with `WRITE = FINANCE_MANAGER + admins`). New `test/finance-authz-matrix.spec.ts` asserts every finance write perm maps to FINANCE_MANAGER + admins only (and reads stay visible to VIEWER). Scope doc updated (Phase B in progress; Finance done). | api+web typecheck 0; `pnpm -w lint` 0 errors; **198/198** unit tests (+3 finance matrix). **Live-proven**: a real FINANCE_MANAGER user POSTs finance/accounts, /vendors, /customers → **201** each with **0 `RbacShadow` divergences** (role & permission models agree → Phase C flip will be a no-op for Finance access). |
| Path 2 RBAC — Phase A (catalog + dual-run harness) | _(post-9.x platform ops)_ | **Foundation for permission-level RBAC; no enforcement change.** New permission catalog registry (`auth/rbac/permission-catalog.ts`) grouped by module (the source of truth for the future role builder). `ROLE_PERMISSIONS` formalised against it, with a consistency unit test (`test/rbac-permissions.spec.ts`). `RbacService.effectivePermissions(roles)` (synchronous — JWT carries built-in-expanded roles) + `GET /tenant/roles/permissions` (catalog + caller's effective permissions). **Dual-run harness**: a log-only `@ShadowPermissions(...)` decorator + a shadow path in `PermissionsGuard` that logs a `RbacShadow` divergence when a role-allowed principal would be denied by the permission model (never blocks). Piloted on `POST hr/employees` / `inventory/products` / `finance/invoices`. Scope doc updated (`docs/permission-rbac-scope.md`) marking Phase A shipped. | api+web typecheck 0; `pnpm -w lint` 0 errors; **195/195** unit tests (+6 rbac-permissions: catalog/role-map consistency, effective-perms, divergence detection). **Live-proven**: `/tenant/roles/permissions` returns 5 domains/14 perms; a shadow-tagged create → 201 with **0 false divergences** (models agree); divergence detection unit-proven (SALES_REP would diverge on `finance:invoice:write`). |
| Platform-owned module entitlements + Path-2 scope | _(post-9.x platform ops)_ | **Module entitlements move from the company to the platform operator.** A company can no longer toggle its own modules: `PATCH /tenant/features/:key` is now SUPER_ADMIN-only, and the real flow is per-company via new SUPER_ADMIN routes `GET /tenants/:id/features` + `PATCH /tenants/:id/features/:key` (`TenantsService.listFeatures`/`setFeature`). Web: the company **Settings → Features** tab is now **read-only** (informational badges, no toggles); the super-admin **company detail dialog** gains a **Modules** section of switches. **Bug fixed:** a SUPER_ADMIN cross-tenant feature toggle 500'd because `AuditService.recordWith` stamped the audit row with the actor's (platform) tenant while the tx GUC was the target tenant → `audit_log` RLS WITH CHECK violation; `recordWith` now takes a `tenantOverride` and `FeatureService.setEnabled` passes the target tenant (actor still from RequestContext). Also added **`docs/permission-rbac-scope.md`** — the phased plan for Path 2 (true permission-level RBAC: catalog → dual-run → per-module tagging+matrix tests → flip). | api+web typecheck 0; `pnpm -w lint` 0 errors; **189/189** unit tests. **Live-proven**: company admin toggle → **403**, read → 200; super-admin disable/enable a company's `projects` module → 200 + state persists + **audit row scoped to that tenant** (67 FEATURE_TOGGLE rows); company admin denied the per-company endpoint (403). |
| Company-admin RBAC — composite custom roles | _(post-9.x platform ops)_ | **A company admin (TENANT_ADMIN) manages their own users and custom roles.** Migration `1726300000000` adds `tenant_role` (tenant-scoped + RLS): a custom role is a named bundle of built-in capability roles (`member_roles`). New `RbacModule`/`RbacService` (`@Global`, Redis-cached): custom-role CRUD (`/tenant/roles`, TENANT_ADMIN), a capability catalog (`/tenant/roles/capabilities`), `effectiveBuiltinRoles` (expand assigned roles → union of built-ins), and `assertAssignable` (reject SUPER_ADMIN / unknown keys). **Enforcement with zero changes to the 265 `@Roles` checks**: `AuthService` expands a user's assigned roles to built-ins at login/refresh, so the access token carries the underlying capabilities. Tenant-scoped user management on `/users` (TENANT_ADMIN, RLS-scoped to own tenant): list, add (validated roles), `PATCH :id/roles` (revokes sessions), `PATCH :id/password`, `PATCH :id/active`. Web: a tabbed **Settings** (Features / Users / Roles, admin-gated) — a **role builder** (capability toggle-chips, create/edit/delete) and a **user admin** table (add user, edit roles, reset password, activate/deactivate, one-time credential reveal). | api+web typecheck 0; `pnpm -w lint` 0 errors; **189/189** unit tests; migration idempotent (2nd run: "No migrations are pending"). **Live-proven**: custom role "Branch Manager"=Inventory+Sales → assigned user's `/me` roles expand to `[INVENTORY_MANAGER,SALES_REP]`; that user POSTs inventory+CRM **201** but finance+HR **403**; SUPER_ADMIN/unknown role assignment rejected. |
| Platform admin v2 — company & user management | _(post-9.x platform ops)_ | **Enterprise client-management for SUPER_ADMIN + account self-service.** Password management: `PATCH /me/password` (any user changes their own — verify-current → re-hash → revoke ALL sessions; new `RefreshTokenService.revokeAllForUser` via a per-user family index) and `PATCH /tenants/:id/users/:userId/password` (SUPER_ADMIN resets a tenant user, sessions revoked). Company management on `/tenants`: `GET /tenants/stats` (total/active/suspended KPIs), `PATCH /tenants/:id` (rename, slug preserved), `PATCH /tenants/:id/plan` (re-apply starter/business/enterprise entitlements), `GET/POST /tenants/:id/users` (list/add users with roles), `PATCH /tenants/:id/users/:userId/status` (activate/deactivate → revoke sessions). **SUPER_ADMIN now sees every module**: features catalog returns all-enabled and `FeatureGuard` bypasses for the platform role. Fixed a latent response-shape bug (UPDATE…RETURNING via `dataSource.query` returns `[rows,count]`) — `setStatus`/`rename` now update-then-`findById`, returning a single object. Web: Companies page gains a KPI strip + clickable rows opening a **company detail dialog** (inline rename, plan apply, suspend/activate, users table with add-user, reset-password, activate/deactivate, one-time credential reveal); topbar account menu gains **Change password**. | api+web typecheck 0; `pnpm -w lint` 0 errors; **189/189** unit tests. **Live-proven on the rebuilt prod stack**: super-admin features all-enabled (16/16); stats correct; rename/plan persisted; add-user → login works; password reset (old 401 / new 200); deactivate → login 401; `/me/password` 204 + re-login with new secret; response shapes single-object. |
| Super-admin company dashboard | _(post-9.x platform ops)_ | **A web console for managing tenants (companies), SUPER_ADMIN-only.** API: `TenantsController` gains `GET /tenants` (list registry, newest first) and `PATCH /tenants/:id/status` (activate/suspend), both behind `@Roles(SUPER_ADMIN)`; `TenantsService.list()`/`setStatus()` query the non-RLS `tenants` table directly. **Suspend is now enforced, not cosmetic**: `AuthService` rejects login *and* refresh when the user's tenant row exists with status≠active — the synthetic platform tenant (super-admin) has no row → treated active, so operators are never locked out. Web: new `/platform/companies` page (role-guarded) — table of every company (name/slug/status badge/created) + suspend/activate actions, and a **New company** dialog that provisions tenant+admin+plan in one call and surfaces the generated credentials to hand over. New sidebar **Platform** section (rendered only for SUPER_ADMIN). | api typecheck 0; web typecheck 0; `pnpm -w lint` 0 errors; unit tests green (tenancy RLS 5/5). **Live-proven on the rebuilt prod stack**: GET /tenants lists 5 companies; suspend → that admin's login returns 401 Unauthorized; reactivate → login issues a token again; super-admin (no tenant row) unaffected. |
| Security hardening H1 — headers/CORS/SAST | _(post-9.x security)_ | **Defensive layer (non-breaking).** Web now sends a full security-header set on every response via `next.config` `headers()`: a Content-Security-Policy (default-src self, frame-ancestors none, object-src none, scoped img/style/script/connect), `X-Frame-Options: DENY`, `X-Content-Type-Options: nosniff`, `Referrer-Policy`, `Permissions-Policy` (camera/mic/geo off), `Strict-Transport-Security` (2y preload), `X-DNS-Prefetch-Control: off`, and `X-Powered-By` removed. API CORS hardened: `CORS_ORIGINS` allowlist as before, but an empty list now **denies cross-origin in production** (was reflect-any-with-credentials). Added `codeql.yml` (SAST for TS/JS + Python → Security tab), complementing the existing dependency/Trivy/pip-audit `security.yml`. | api typecheck 0; web build compiles; deployed live — headers verified present, evil-origin request gets no ACAO (denied). |
| Pharmacy P5 — reports & analytics | `ckpt/pharmacy.5-stable` | **The analytics layer — final pharmacy chunk.** `PharmacyReportsService` (read-only aggregations, no migration): KPI **dashboard** (drugs, stock value, expiring value-at-risk, dispenses, revenue/COGS/**margin %**), **sales** by channel, **consumption** (top drugs), per-drug **margin**, **ABC/Pareto** classification (`classifyAbc`, pure + tested), stock **valuation**, and **expiry-bucket** value-at-risk. 7 report endpoints. Web: the Reports section is now a rich **dashboard** (`reports-dashboard.tsx`) — KPI tiles, sales-by-channel, top consumption, ABC distribution, expiry buckets, valuation, and a client-side **controlled-register CSV export**. **Deployed live + smoke-tested** (revenue 185,900 / margin 19.6%; ABC: Paracetamol B@81.2%, Tramadol C; all 7 reports correct). **Pharmacy module complete: P1–P6 all live.** | api typecheck 0; web typecheck 0; `pnpm -w lint` 0 errors; **21/21** pharmacy unit tests (added 2 ABC); no migration (read-only); live end-to-end on the running stack. |
| Pharmacy P4 — hospital + wholesale | `ckpt/pharmacy.4-stable` | **Hospital ward issues + wholesale B2B orders + quantity price tiers.** Migration `1726200000000` adds `pharmacy_ward_requisition(+item)`, `pharmacy_sales_order(+item)`, `pharmacy_price_tier`. Hospital: a ward raises a requisition → submit → approve → **issue** rings a HOSPITAL_ISSUE dispense (FEFO + GL) charged to the ward/patient on credit. Wholesale: a B2B sales order → confirm → **fulfil** rings a WHOLESALE dispense. `resolveTierPrice` (pure, tested) gives quantity-break pricing per drug, auto-applied during dispense when a line has no explicit price. Refactored the dispense into `createDispenseInTx` so issue/fulfil compose it atomically. Web: **Ward issues** + **Sales orders** rail sections (list + create + status actions: submit/approve/issue, confirm/fulfil/cancel) and a **price-tier editor** on the drug detail (3 components via parallel agents). Reuses the P2 `pharmacy.dispense_completed` GL path (no new event). **Deployed live + smoke-tested**: ward WREQ-000001 issued; wholesale PSO-000001 fulfilled at the bulk tier (120 × 1100 = 132,000); all 4 dispense channels exercised. | api typecheck 0; web typecheck 0; `pnpm -w lint` 0 errors; **19/19** pharmacy unit tests (added 3 tier-pricing); migration idempotent (dev + prod); live end-to-end on the running stack. |
| Pharmacy P3 — adjustments + procurement | `ckpt/pharmacy.3-stable` | **Return-to-vendor, expiry write-off, stock adjustment — all valued + GL-posted; plus a PO tie-in.** Migration `1726100000000` adds `pharmacy_stock_adjustment(+item)` and `pharmacy_receipt.po_id`. New `PharmacyAdjustmentService`: `returnToVendor` / `writeOff` / **`writeOffExpired`** (one-click purge of every expired lot) / `adjustStock` — each decrements a chosen lot via the valued ledger (WAVG) and emits `pharmacy.stock_adjusted`; `PharmacyGlConsumer` extended to post Dr write-off\|clearing / Cr inventory (reversed for a write-up). `receiveBatch` now accepts `poId` and rolls received quantities onto the inventory PO (+refreshes status) — the batch-aware GRN. Web: a new **Adjustments** rail section (history + "Write off expired") and per-lot actions (write-off / return-to-vendor / adjust) on the batch detail. New shared event `pharmacy.stock_adjusted.v1`. **Deployed live + smoke-tested** (RTV/WOF/ADJUST/expiry-purge correct, lot math 188→175). | api typecheck 0; web typecheck 0; `pnpm -w lint` 0 errors; **16/16** pharmacy unit tests (added 4 adjustment-voucher); migration idempotent (dev + prod); live end-to-end on the running stack. |
| Pharmacy P6 deploy + fix | `ckpt/pharmacy.6-stable` | **Deployed live to the running stack** (migrated prod DB: 11 pharmacy tables, idempotent; enabled `pharmacy` for the Acme Demo Co tenant; rebuilt api+web+worker). Fixed a transaction-isolation bug found by the live smoke test: `createDrug`/`updateDrug` called `getDrug` which opened a NEW transaction that could not see the still-uncommitted write → "Drug not found"; extracted `getDrugInTx(m,id)` read within the same tx. | Live end-to-end on prod stack: login → create 2 drugs (incl. a NARCOTIC) → receive batches → retail + Rx dispenses (priced + FEFO COGS correct: total 189 / cogs 96) → controlled register auto-logged (OUT 10, bal 90) → near-expiry report. api typecheck 0. |
| Pharmacy P6 — web workspace | `ckpt/pharmacy.6-stable` | **Pharmacy three-pane web console** (`/pharmacy`, gated by the `pharmacy` feature in the sidebar nav). Rail: Drug master / Dispense / Dispense history / Batches & expiry / Controlled register / Reports / Settings, with a KPI strip (drugs · below-reorder · expiring ≤90d · controlled). Drug master: searchable list + `DrugDetail` (full edit + delete + per-drug batches w/ expiry badges + stock stats) + `NewDrugDialog`. `ReceiveBatchDialog` (multi-line lot+expiry goods-in). `DispenseCounter` — a POS-style till: build a cart, live integer-minor totals, complete → FEFO consume + GL. `DispenseDetail` with line/lot breakdown + **return/reverse**. Controlled-substance register view. Reports (near-expiry value-at-risk). `PharmacySettings` (mode/policies + 8 GL-account selectors). New `components/pharmacy/*` (built via 4 parallel agents). | `pnpm --filter @app/web typecheck` 0; `pnpm -w lint` 0 errors; web prod build compiles `/pharmacy` (18.5 kB). |
| Pharmacy P2 — dispensing & GL | `ckpt/pharmacy.2-stable` | **Dispensing/sales engine with ledger posting.** `pharmacy-dispense.service` rings a dispense (RETAIL_SALE/RX/HOSPITAL_ISSUE/WHOLESALE): each line priced (qty×price − discount + tax bp), stock consumed **FEFO** (WAVG COGS), **controlled/scheduled drugs append to the register**, header totals rolled up, and a `pharmacy.dispense_completed` event written to the **outbox**. `PharmacyGlConsumer` (idempotent, gated by `WORKER_REACTIONS_ENABLED`) posts the balanced JV via FinanceService — Dr debtor (clearing for cash/card, **receivable** for credit/insurance, with insurer co-pay split) / Cr revenue [+ tax]; Dr COGS / Cr inventory — **reversed on return**. Added pharmacy GL account-map config, dispense list/detail, returns (restock + reversing voucher), controlled-register report. New shared event `pharmacy.dispense_completed.v1`. | `pnpm --filter @app/api typecheck` 0; `pnpm -w lint` 0 errors; **12/12** pharmacy unit tests (6 FEFO + 6 GL-voucher: balance, cash/credit/insurance split, return reversal, skip-when-unconfigured). No migration (P1 schema). |
| Pharmacy P1 — foundation | `ckpt/pharmacy.1-stable` | **New configurable Pharmacy module (retail/hospital/wholesale), built on inventory + finance.** Migration `1726000000000-Pharmacy` adds the full pharma schema (config, gl_config, drug master, **stock lots w/ batch+expiry**, lot movements, receipts, dispenses, **controlled-substance register**) — all RLS + integer minor units. P1 services: per-tenant config; drug master CRUD (a drug 1:1-enriches an `inventory_product`); **batch receipt** that creates lots and pushes value through the shared inventory valued ledger (one source of stock truth); **FEFO** allocator (`allocateFefo`, pure + unit-tested) + `consumeFefoInTx` dispensing seam returning WAVG COGS; near-expiry / expired / lot-valuation reports. Feature `pharmacy` (dependsOn inventory+finance) added to the registry + business plan; gated controller; wired into app. Dispensing/sales/GL-posting/controlled-write land in P2. | `pnpm --filter @app/api typecheck` 0; `pnpm -w lint` 0 errors; 6/6 FEFO unit tests green; `migration:run` applied (11 pharmacy tables) then **idempotent** (2nd run "No migrations are pending"). |
| Help Desk three-pane | _(post-9.1 feature)_ | **Help Desk agent console folded into the three-pane shell — the rollout's final module.** `/helpdesk` rail: Tickets / Teams / SLA policies / Canned replies, with the overview KPI strip retained. Tickets: filterable list pane + a `TicketDetail` pane (the full conversation + composer + actions sidebar, adapted from the old `[id]` page). Teams / SLA / Canned each render their existing settings-dialog panels (now exported `TeamsTab`/`SlaTab`/`CannedTab`) as full panes with their existing CRUD. `helpdesk/[id]` kept as a thin deep-link wrapper over `TicketDetail`. Customer storefront portal untouched. | typecheck/lint 0; web prod build compiles `/helpdesk` + `/helpdesk/[id]`; API unchanged. **Three-pane rollout complete (11 modules).** |
| Ecommerce admin rail-shell | _(post-9.1 UX)_ | **Ecommerce admin reorganised onto the three-pane rail shell** — the public storefront `/shop/[slug]` is left untouched (operational, like the POS till). `/ecommerce` now has the left RailItem rail (Storefront / Products / Collections / Discounts / Reviews / Shipping / Orders / Accounting) + a "View store" link, with each existing self-contained admin panel (ProductsAdmin, OrdersAdmin, etc. — already full CRUD) rendered in a scrolling content pane; a section `<select>` replaces the rail below `lg`. No panel logic changed (they already do list + create + edit/delete inline); this is the consistent shell + nav. | typecheck/lint 0; web prod build compiles `/ecommerce`; storefront + API unchanged. |
| Projects three-pane | _(post-9.1 feature)_ | **Projects web retrofit to the three-pane shell** (backend already full-CRUD + camelCase — web-only). Rebuilt `/projects` as one three-pane (rail: Projects / Reports) + KPI strip, replacing the portfolio/timesheet tabs + detail modal. Project detail pane: costing tiles + **edit + status + delete** (newly surfaced) and nested CRUD — tasks (add/edit status+priority/delete), members (add/delete with rates), time entries (log/**approve+reject**/delete), expenses (add/delete) — most of which the API supported but the UI never exposed. Reports pane: portfolio + timesheet summaries. Removed the dead project-detail-dialog. | `pnpm -w typecheck`/`lint` 0; web prod build compiles `/projects`; API unchanged. |
| Fixed Assets three-pane | _(post-9.1 feature)_ | **Assets web retrofit to the three-pane shell** (backend already CRUD-complete + camelCase — web-only). Rebuilt `/assets` as one three-pane (rail: Assets / Categories / Maintenance / Reports / Costing) replacing the tabbed page. Asset detail with **edit** (name/desc/location/serial/supplier/notes — newly surfaced) + lifecycle (activate/dispose/write-off) + cost tiles + depreciation history + maintenance + schedule preview; category detail with **edit + delete** (newly surfaced PATCH/DELETE); maintenance log + create; reports (register by category, depreciation runs, upcoming maintenance) + run-depreciation; GL config. Removed the dead asset-detail-dialog. | `pnpm -w typecheck`/`lint` 0; web prod build compiles `/assets`; API unchanged. |
| Production three-pane | _(post-9.1 feature)_ | **Manufacturing web retrofit to the three-pane shell** (backend already full-CRUD + camelCase — web-only). Rebuilt `/production` as one three-pane (rail: Work Orders / Bills of Materials / Work Centers / Attributes / Reports / Costing) replacing the tabbed page. Work-order detail with lifecycle (plan/release/issue/complete/cancel) + cost breakdown + **edit** (priority/overhead/dates/notes); BOM detail with components/operations + **edit + status + delete** (newly surfaced); work-center **edit + delete** (newly surfaced); attribute create/delete; production reports (shortages/output/WIP); GL costing config. Reused all existing create dialogs; removed the dead order-detail-dialog. | `pnpm -w typecheck`/`lint` 0; web prod build compiles `/production`; API unchanged. |
| Collapsible sidebar | _(post-9.1 UX)_ | The main left nav collapses to a 64px icon rail (toggle in the header + a bottom Collapse button), persisted in `localStorage`; labels/section headers hide when collapsed (icons keep `title` tooltips), width animates. More screen for the three-pane workspaces. | typecheck/lint 0; web build green. |
| POS back-office three-pane | _(post-9.1 feature)_ | **POS back-office three-pane at `/pos/manage`** — the offline-capable cashier **till stays untouched** at `/pos` (a POS terminal is not a three-pane; offline PWA + sale queue preserved). POS backend was already full-CRUD, so this is web-only and fills the missing admin/reporting surface: Registers (list + **edit/delete** + create), Shifts (history + report + close-out), Sales (history + detail with lines/tenders + **void/refund** reusing ReturnDialog + receipt), Reports (daily summary + top products), Settings (branding + GL config). Added a "Back-office" link in the till header and a "Back to till" link. No backend changes; no offline changes. | `pnpm -w typecheck`/`lint` 0 (2 pre-existing finance warnings); web prod build compiles `/pos` + `/pos/manage`; till + offline untouched. |
| HR three-pane | _(post-9.1 feature)_ | **HR web retrofit to the three-pane shell** (backend CRUD landed in 929ebcc). Rebuilt `/hr` as one three-pane workspace (rail: Employees / Attendance / Leave / Payroll / Performance / Policies / Organization / Reports + KPI strip) replacing 7 tab pages. Employee detail panel = full profile + edit + education/experience + **leave balances** + **lifecycle history** + **documents** (all newly surfaced in UI). Self-contained panels: Attendance (daily bulk grid + monthly summary), Leave (requests approve/reject + leave-type edit/delete), Payroll (salary-component edit/delete + runs approve/pay + payslip detail), Performance (review submit + goal progress/status), Policies (status + custom fields), **Organization** (departments/designations/positions full CRUD), HR Reports (headcount/payroll/leave). Reused all existing HR dialogs. `employees/[id]` kept as a deep-link wrapper; removed old sub-pages + `hr-tabs`. | `pnpm -w typecheck`/`lint` 0 (2 pre-existing finance `any` warnings); web prod build compiles `/hr`; API suite 168/168 (HR backend CRUD proven by hr-crud-e2e.sh 11/11 in 929ebcc). |
| Finance three-pane | _(post-9.1 feature)_ | **Finance retrofit to the three-pane shell** (web-only — the accounting API already exposed the needed operations). Rebuilt `/finance` as one three-pane workspace (rail grouped: Invoices / Customers / Bills / Vendors · Chart of Accounts / Journal / General Ledger / Reconciliation · Reports / Periods / Currencies / Recurring / Cost Centers) replacing 11 tab pages. Detail panels: invoice (lines + receive payment), bill (lines + payments + pay), **account edit** (newly surfaced PATCH — name/control/bank), transaction (entries + post/reverse/delete-draft), period (lock/reopen + year-end close), recurring (run). Full-pane tools: General Ledger (account ledger), Reconciliation (mark cleared + import + auto-match), a sub-tabbed Reports dashboard (trial balance / balance sheet / income / cash flow / AR + AP aging), and a Currencies & exchange-rates panel. Reused all existing create dialogs (invoice/bill/customer/vendor/account/journal/recurring/cost-center) + year-end-close + import-statement. Removed old sub-pages + `finance-tabs`. | `pnpm -w typecheck`/`lint` 0 (2 cosmetic `any` warnings on reused-dialog casts); web prod build compiles `/finance`; API unchanged (168/168 still green). |
| Inventory three-pane + CRUD | _(post-9.1 feature)_ | **Inventory retrofit to the three-pane shell with completed CRUD.** Backend: added product **update + delete** (delete blocked while stock on hand), warehouse **update**, and camelCase **detail endpoints** for GRN / issue / MRN / gate pass (header + line items); normalised `getRequisition`/`getPurchaseOrder` to camelCase; added a shared `rowsOf()` for the `UPDATE…RETURNING`→`[rows,count]` quirk. Web: rebuilt `/inventory` as one three-pane workspace (rail: Products / Categories / Warehouses / Requisitions / Purchase Orders / Receipts(GRN) / Gate Passes / Issues / Returns(MRN) / Reports + a KPI strip) replacing 7 tab pages. Product detail with full edit/delete + images + movements + valued ledger; requisition/PO detail with state actions (submit/approve/cancel/issue, approve/cancel/receive); read-only posted-doc detail for GRN/issue/MRN; gate-pass detail + close; compact create dialogs for all six document types (shared `LineItemsEditor`); warehouse edit; stock-valuation + reorder reports. Removed old sub-pages + `inventory-tabs`. | `pnpm -w typecheck`/`lint` 0; new `inventory-crud-e2e.sh` **18/18** (product update/delete + stock-guard, warehouse update, camelCase requisition/PO/GRN/issue/MRN/gate-pass detail through a real procurement+issuance flow); full API suite 168/168; web prod build compiles. |
| CRM three-pane + CRUD | _(post-9.1 feature)_ | **CRM retrofit to the three-pane shell with complete CRUD.** Backend: added the missing operations — account get/update/delete, contact update/delete, deal update/delete, lead update/delete, activity update/delete (soft-delete, tenant-scoped) + DTOs + routes; gave clients/contacts **camelCase mappers** (`mapClientRow`/`mapContactRow`), fixing a latent casing bug where the API returned `company_name` but the web read `companyName`. Fixed the recurring `UPDATE…RETURNING` → `[rows,count]` quirk across all CRM updates via a shared `rowsOf()` (this also repaired the pre-existing `updateLeadStatus`/`completeActivity`, which silently returned empty bodies). Web: rebuilt `/crm` as one three-pane workspace (rail: Accounts/Leads/Pipeline/Activities/Reports + KPI strip) replacing 5 tab pages; detail panels with edit/delete for each entity, inline Contacts CRUD, a reusable Activity timeline (log/complete/delete), pipeline grouped by stage, and the sales-reports dashboard. Removed the old sub-pages + `crm-tabs`. | `pnpm -w typecheck`/`lint` 0; new `crm-crud-e2e.sh` **27/27** (account/contact/deal/lead/activity full CRUD, camelCase reads, lead convert, stage change, activity complete, tenant isolation 404); full API suite 168/168; web prod build compiles. |
| Subscriptions | _(post-9.1 feature)_ | **Subscriptions & recurring billing module** (`apps/api/src/modules/subscriptions`). Plans (price per interval, trials, setup fees, tax, archive) → subscriptions for a subscriber (AUTO auto-charge / MANUAL invoice). A **billing engine** (`processBillingInTx`) generates an invoice each cycle, advances the period, auto-collects (AUTO) or leaves it OPEN (MANUAL), and **duns** overdue invoices → PAST_DUE → cancel after MAX attempts (uncollectible). Trials defer the first invoice; cancel-at-period-end ends cleanly at the boundary; pause/resume; change plan/quantity (next cycle). Cross-tenant **billing scheduler** (5-min heartbeat, `WORKER_REACTIONS_ENABLED`). Paid invoices post a balanced JV to the GL (`SubscriptionsGlConsumer` + reusable `GlAccountsCard`); customer emails (receipt / dunning / cancellation) + in-app finance alerts via the outbox. MRR/ARR/churn metrics. Customer self-service portal (`/shop/:slug/subscriptions`, reuses storefront customer auth). Web: admin (metrics, subscriptions, plans, invoices, accounting tabs) + detail + storefront pages. Feature `subscriptions` (deps `finance`); `subscription.*` events; 5 new tables (RLS, integer minor units, idempotent migration). | `pnpm -w typecheck`/`lint` 0; 8 unit (MRR normalisation, interval literal, balanced GL voucher, emails) + **36/36 e2e** (auto-bill+collect, re-bill+period advance, dunning→PAST_DUE, manual pay reactivates, dunning-to-exhaustion cancel, trial-then-bill, cancel-at-period-end, pause-skips/resume, feature-gating 403, tenant isolation 404); full API suite 168/168; web prod build compiles all 4 routes; migration idempotent. |
| Product images (feature) | _(post-9.1 feature)_ | **Products can now have one or more images, shown as thumbnails in the dashboard.** Reuses the existing `app_attachment` store + `StorageService`. Migration `ProductImages` adds `inventory_product_image` (links a product → an attachment, with sort + a single **primary** via a partial-unique index; composite FKs cascade). `InventoryService` (now injecting `StorageService`): `addProductImage` (multipart; first image becomes primary), `listProductImages`, `getProductImage` (streamed), `setPrimaryImage`, `removeProductImage` (promotes the next image to primary on delete). Endpoints under `/inventory/products/:id/images[/:imageId][/primary]` (writes gated to INVENTORY_MANAGER/admin; binary served via `StreamableFile`). The product SELECT now carries `primaryImageId` + `imageCount`. Web: an `AuthImage` component (fetches protected images as a blob → object URL via a new `apiBlob`, since `<img>` can't send the bearer), a **product-images manager dialog** (upload multiple, gallery, set-primary, delete), and a **primary-image thumbnail** + image button in the inventory product list. | `pnpm -w typecheck/lint` 0; vitest **139/139** (unchanged — image paths are IO over the proven storage/streaming pattern); Next prod build green; migration applied to dev then re-ran → "No migrations are pending". |
| Offline-first POS (PWA + sync) (feature) | _(post-9.1 feature)_ | **The POS keeps selling with no internet / server down.** Made the web app a **PWA**: `app/manifest.ts`, a hand-rolled `public/sw.js` (cache-first immutable build assets; network-first navigations with cache fallback; **never caches the cross-origin API**), an `/offline.html` shell, an SVG icon, and `PwaRegister` wired into the root layout — so the POS screen loads with zero internet after one online visit. Client offline layer (`lib/pos-offline.ts`, localStorage): caches the **catalogue + active register + open-shift snapshot** while online, and a durable **sale queue**. Each offline sale gets a client `crypto.randomUUID()` **Idempotency-Key**; `lib/pos-sync.ts` replays the queue to the existing `/pos/sales` endpoint — the **already-built Idempotency-Key interceptor** guarantees exactly-once (no double-rings). `apiPost` now forwards an idempotency key. The POS page: rings sales offline (instant provisional receipt printed from local data), falls back to cached catalogue/shift, and an **OfflineBar** shows online/offline + pending-sync count + auto-sync on reconnect + a **review list** for any sale a reconcile-time check rejects (per the chosen *allow + reconcile* policy). Server-down-while-online is also queued (network-error → enqueue). | `pnpm -w typecheck/lint` 0; Next prod build green (PWA `/manifest.webmanifest` emitted, `/pos` compiles). Backend unchanged (139 tests still pass) — offline safety rides on the existing idempotency interceptor (idempotency-e2e green). |
| Project Management & Timesheets (feature) | _(post-9.1 feature)_ | **New Projects module** integrating CRM clients + HR employees (raw SQL via `tenantTx`, RLS, integer minor units). Migration `Projects` adds `project_doc_seq` + 5 tenant-scoped tables: `project` (client/manager FKs, budget, billing type), `project_member` (hourly **cost + bill rates**, one per employee), `project_task` (status board + priority + estimate), `project_time_entry` (logged in **minutes**; DRAFT→SUBMITTED→APPROVED — costed + billed from the member's rate at approval), `project_expense`. `ProjectsService`/`ProjectsController`: project CRUD + status; member add/remove; task CRUD + status; **time log → approve** (computes cost/bill); expense CRUD; and **project costing** (labour + expenses vs budget, billable, remaining, hours) + **portfolio** and **timesheet-by-employee** reports. Pure `projects.util`: `entryCost`/`entryBill` (minutes×rate, non-billable→0), `projectActuals` roll-up, `budgetRemaining`. Feature `projects` + keys; writes gated to HR_MANAGER/admin. Web: a Projects page (portfolio + timesheet tabs, over-budget/billable/hours stats) + create dialog + a project-detail dialog (costing summary + Tasks/Time/Members/Expenses tabs with inline add + task-status + time-approve). | `pnpm -w typecheck/lint` 0; vitest **139/139** (projects suite 4: entryCost/entryBill, projectActuals labour+expense roll-up, budgetRemaining). **projects-e2e 12/12** (project → member rates → task → log time → approve costs 2h×rate → costing rolls labour+expense vs budget → portfolio/timesheet) wired into `test:e2e`. Next prod build green (`/projects`). Migration applied to dev then re-ran → "No migrations are pending". |
| E2E coverage for POS / Production / Assets (hardening) | _(post-9.1 hardening)_ | **Closed the e2e gap that let a POS 500 ship.** Added `pos-e2e.sh`, `production-e2e.sh`, `assets-e2e.sh` (wired into `test:e2e`), each booting a dedicated API instance against a freshly-provisioned tenant and asserting the full real SQL/lifecycle path: **POS** — register → open shift → ring a sale (stock decrement + COGS capture, the path that regressed) → underpaid-rejected (422) → partial refund (restock + PARTIALLY_REFUNDED) → close shift with cash reconciliation → tenant isolation. **Production** — work center → BOM (components + routing) → order → release → complete (backflush + WAVG material + operation + overhead cost, FG received at unit cost, ledger moves) → one-active-BOM (409). **Assets** — category → asset → activate → straight-line depreciation run → accumulated/net-book-value → idempotent re-run (422) → disposal gain/loss → maintenance + upcoming-due. Idempotent + self-cleaning. | **POS 15/15, Production 15/15, Assets 12/12** against the live dev stack (42 new assertions). Each verifies stock, cost, status, outbox, and isolation through the real API — the class of SQL wiring bug unit tests miss. |
| Close the accounting loop: POS + production → GL (feature) | _(post-9.1 feature)_ | **Operational events now post to the general ledger**, extending the depreciation→GL pattern across the suite. **POS**: migration `PosGlConfig` (clearing/revenue/tax/cogs/inventory accounts) + `GET/PUT /pos/gl-config`; `PosGlConsumer` turns each `pos.sale_completed` into a balanced JV — **Dr clearing (total), Cr revenue (total−tax) [+ Cr tax]; Dr COGS, Cr inventory** — reversed for a RETURN; reads the sale's tax/total/cogs in-tx; idempotent. **Production**: migration `ProductionGlConfig` (fg-inventory/raw-materials/labour/overhead) + `GET/PUT /production/gl-config`; `ProductionGlConsumer` turns each `production.order_completed` into **Dr finished-goods inventory; Cr raw materials, labour, overhead** (zero components omitted, stays balanced). Both consumers reuse `FinanceService.postJournalInTx`, post inside the idempotent tenant tx (exactly once per event), skip gracefully when unconfigured, and are gated by `WORKER_REACTIONS_ENABLED`. Pure builders `posSaleVoucher` / `productionVoucher` (balanced-or-null). Web: a reusable `GlAccountsCard` (leaf-account picker per slot) on a new Production **Costing → GL** tab and a POS admin card. | `pnpm -w typecheck/lint` 0; vitest **135/135** (POS suite +3, production +2: voucher balance, RETURN reversal, tax-fold, zero-component skip, null-when-unconfigured). Next prod build green. Migrations `PosGlConfig` + `ProductionGlConfig` applied to dev then re-ran → "No migrations are pending". |
| Assets → finance GL posting for depreciation (feature) | _(post-9.1 feature)_ | **Closed the loop from depreciation to the general ledger.** New `AssetGlConsumer` subscribes to `asset.depreciation_posted` (via the IdempotentConsumer; gated by `WORKER_REACTIONS_ENABLED`) and posts a **journal voucher — Dr depreciation expense, Cr accumulated depreciation** — through the new public `FinanceService.postJournalInTx(m, dto)`, **inside the consumer's idempotent tenant transaction** so the voucher commits exactly once per run. Accounts come from a per-tenant **GL config** (`asset_gl_config`, one row/tenant, composite FKs to `finance_account`) set via `GET/PUT /assets/gl-config`; when unset (or amount zero) the consumer **skips gracefully** — the asset depreciation entries are unaffected. Pure `depreciationVoucher` builder (null-or-balanced). `FinanceModule` now exports `FinanceService`; `AssetsModule` imports it. Web: a **GL-posting accounts** card on the Fixed Assets → Depreciation tab (pick expense + accumulated accounts from the leaf chart of accounts, with a configured/not-configured badge). | `pnpm -w typecheck/lint` 0; vitest **130/130** (assets suite 7: + `depreciationVoucher` balanced/null cases). Next prod build green. Migration `AssetGlConfig` applied to dev then re-ran → "No migrations are pending". |
| Fixed Asset Management (feature) | _(post-9.1 feature)_ | **New enterprise Fixed Assets module** (raw SQL via `tenantTx`, RLS, integer minor units). Migration `Assets` adds `asset_doc_seq` + 5 tenant-scoped tables: `asset_category` (default depreciation policy), `asset` (acquisition cost + policy + accumulated depreciation; **hr_employee custodian** composite FK), `asset_depreciation_run` (+ `asset_depreciation`, **unique on (asset, period)** so a period is never depreciated twice), `asset_maintenance`. New `AssetsService`/`AssetsController`: category CRUD; asset CRUD + **activate / dispose (gain-or-loss vs net book value) / write-off**; **depreciation run** (per-period, idempotent — iterates eligible active assets, posts one entry each, advances accumulated, emits `asset.depreciation_posted.v1`); depreciation **schedule preview**; maintenance log + **upcoming-due**; reports (register by category with net book value). Pure `assets.util`: `periodDepreciation` (straight-line + **double-declining**, salvage clamp, final-period catch-up to land exactly on salvage), `depreciationSchedule`, `salvageFromPct`. New event contract + `asset` event domain. Feature `assets` + keys; writes gated to FINANCE_MANAGER/admin. Web: a Fixed Assets page (Assets / Categories / Depreciation / Maintenance tabs) with create dialogs, an asset-detail dialog (cost/accumulated/book-value, depreciation history + projected schedule, activate/dispose/write-off), a run-depreciation dialog, register-by-category report, and net-book-value/maintenance-due stats. | `pnpm -w typecheck/lint` 0; vitest **129/129** (assets suite 6: salvage, straight-line flat + clamp + final catch-up, NONE, full schedule sums to cost, double-declining tapers to salvage). Next prod build green (`/assets`). Migration applied to dev DB then re-ran → "No migrations are pending". |
| Notifications, completed (feature) | _(post-9.1 feature)_ | **Elevated the minimal notifications into a complete module.** Migration `NotificationsEnterprise` (additive/idempotent) enriches `notification` with **severity / category / deep-link / archived_at** and adds **`notification_preference`** (per-user, per-category in-app + email channel prefs). `NotificationsService` keeps `listUnread`/`markRead` (e2e + unit contract) and gains a **paginated feed** (unread/all/archived + category), **unread-count**, mark-unread, **mark-all-read**, **archive**, delete, **get/set preferences**, plus consumer helpers (`resolveRoleRecipients`, in-app/email pref gates). `createInTx` now respects the recipient's in-app preference. The **consumer fans out 7 event types** (was 1): deal-won + lead-converted → the owning user; low-stock, invoice-paid, leave-approved, payroll, production-complete → the relevant **role** (idempotent per recipient via the source-event unique index); email is **opt-in + best-effort**. Pure `notifications.util` builds each message (category/severity/title/body/link). Realtime bridge expanded to nudge clients on the new events. Controller surface fully RESTful; recipient is always the JWT principal. Web: a **notification bell** in the topbar (unread badge, 30s poll, recent dropdown, mark-all-read) + a **notification center** page (All/Unread/Archived, severity icons, per-item read/unread/archive/delete) + a **channel-preferences** panel. | `pnpm -w typecheck/lint` 0; vitest **123/123** (notifications-util 4 + the existing notifications service/consumer suite still green). Next prod build green (`/notifications`). Migration applied to dev DB then re-ran → "No migrations are pending". GET `/notifications` shape preserved (`{data,meta}` → e2e `.data` list contract intact). |
| Manufacturing / Production (feature) | _(post-9.1 feature)_ | **New enterprise Production module** on top of the valued inventory ledger (raw SQL via `tenantTx`, RLS, integer minor units). Migration `Production` adds `production_doc_seq` + 9 tenant-scoped tables: `production_work_center` (hourly cost rate), `production_bom`(+`_line` components, `_operation` routing; partial-unique **one ACTIVE BOM per product**), `production_order`(+`_material`, `_operation`), and **EAV custom attributes** `production_attribute`(+`_value`) — the "preset + customized attributes" surface. New `ProductionService`/`ProductionController`: work-center CRUD; BOM CRUD with **standard-cost roll-up** (components×scrap + operations + overhead) + activate/archive; custom-attribute CRUD; and the order lifecycle — **create from BOM** (materials scaled to planned qty, operations + overhead snapshotted) or ad-hoc → plan → release → **issue materials** (each decrements stock through `InventoryDocsService.applyStockMovement`, capturing actual WAVG cost) → **complete** (backflushes remaining materials, costs operations, **receives the finished good into stock at the computed unit cost**, emits `production.order_completed.v1`) → cancel (restocks issued materials). Reports: WIP, output-by-product, material shortages. New event contract + `production` event domain. Feature `production` (dependsOn `inventory`) + keys; writes gated to INVENTORY_MANAGER/admin. Web: a Manufacturing page (Orders / BOMs / Work centers / Attributes tabs) with create dialogs, an order-detail dialog driving the full lifecycle + live cost breakdown, WIP/shortage stats, and dynamic custom-attribute inputs. | `pnpm -w typecheck/lint` 0; vitest **119/119** (production suite 4: `scaledRequiredQty` scrap/round-up, `operationCostMinor`, `rollUpCost` overhead+unit, `bomStandardCost` roll-up). Next prod build green (`/production` route). Migration applied to dev DB then re-ran → "No migrations are pending". |
| POS: cashier terminal UX + card terminals (feature) | _(post-9.1 feature)_ | **Full cashier terminal + pluggable card-payment terminals.** Backend: migration `PosTerminal` adds `pos_register.card_terminal_provider`/`card_terminal_url` + `pos_payment.card_scheme`/`card_last4` (additive, idempotent). New `PaymentTerminalService` with three providers — **NONE** (manual ref), **SIMULATED** (built-in deterministic approver, no randomness → testable), **BRIDGE** (POSTs the charge to a local terminal agent on the cashier's PC over a documented contract; ADR-004-resilient: bounded 60s timeout + circuit breaker + typed failure path, **deliberately not auto-retried** since a card charge isn't idempotent). `POST /pos/registers/:id/charge` returns the approval; register CRUD + the CARD tender now carry scheme/last-4 for the receipt. Web: the POS page is rebuilt into a proper **cashier terminal** — product grid + **barcode/SKU scan-to-add** + search, cart with qty steppers/line totals, walk-in or CRM customer, order discount, live totals; a **payment dialog** (split tenders, numeric keypad, quick-cash buttons, **charge-on-terminal**, change due), a **printable receipt**, **park/recall**, **void**, a **refund dialog** (per-line quantities), and **shift open/close** with the cash-count variance inline. New register dialog configures the till's warehouse + card terminal. | `pnpm -w typecheck/lint` 0; vitest **115/115** (pos suite 7: + `simulateTerminalCharge` determinism/format). Next.js prod build green (`/pos` route compiles). Migration applied to the dev DB then re-ran → "No migrations are pending". |
| POS: point of sale (feature) | _(post-9.1 feature)_ | **New Point of Sale module** (raw SQL via `tenantTx`, RLS, integer minor units). Migration `Pos` adds `pos_doc_seq` + 5 tenant-scoped tables: `pos_register` (till, optional `inventory_warehouse` link), `pos_shift` (cash session — opening float → close-out reconciliation; partial-unique index enforces **one OPEN shift per register**), `pos_sale`(+`_line`, `pos_payment`) covering SALE/RETURN and PARKED→COMPLETED→VOIDED/REFUNDED. New `PosService`/`PosController`: register CRUD; **open/close shift** with cash-drawer reconciliation (`expectedCash = float + cash sales − change − cash refunds`, variance) + X/Z report; **createSale** (multi-line, per-line discount+tax, multi-tender, change due, walk-in or `crm_client`) that **decrements stock through the inventory valued ledger** (new public `InventoryDocsService.applyStockMovement` reused → COGS captured per line); **park + complete**, **void** (restock), **refund** (RETURN sale restocks + pays back, marks original REFUNDED/PARTIALLY_REFUNDED); reports (daily summary + tender mix, top products). Emits `pos.sale_completed.v1` via the **outbox** (new shared event contract + `pos` event domain). Feature `pos` (dependsOn `inventory`) + keys `pos.registers/shifts/sales/reports`. Web: POS terminal page (register/shift, cart, tender→change, this-shift sales) + nav entry + types. | `pnpm -w typecheck/lint` 0; vitest **114/114** (pos suite 6: `computeLine` discount-clamp + integer tax, `computeSaleTotals` line+order discount + clamp, `changeMinor`, `expectedCash`/`variance`). Migration **applied to the live dev DB then re-ran → "No migrations are pending"** (idempotent). |
| HR: employee photos + attachment store (feature) | _(post-9.1 feature)_ | **Employee photos on registration + a reusable binary store.** New `app_attachment` table (RLS, `bytea`) with a global `StorageService` (`put`/`putInTx`/`get`, 8 MiB cap) — a DB-backed object store, swappable for MinIO/S3 later behind the same API (no new dependency). HR: `photo_ref` now wired through create (DTO + INSERT + `EMP_COLS` + view), plus `POST /hr/employees/:id/photo` (multipart, image-only) → stores the bytes atomically and repoints `photo_ref` (clearing the prior attachment), and `GET /hr/employees/:id/photo` streaming the raw image. The success-envelope interceptor now passes `StreamableFile`/`Buffer` through untouched so binary isn't corrupted. Web: photo file-input in the New-Employee dialog (uploads after create via a new `apiUpload` multipart helper). | `pnpm -w typecheck/lint` 0; vitest 114/114 green. Migration applied to the live dev DB + idempotent re-run. Fixed a real shape bug: the photo UPDATE…RETURNING now unwraps via `returningRows` ([rows,count]). |
| Sales: quote-to-order (feature) | _(post-9.1 feature)_ | **New Sales module — quotations & sales orders** (raw SQL via `tenantTx`, RLS, integer minor units, like CRM/inventory). Migration `Sales` adds 5 tenant-scoped tables + a `sales_doc_seq` numbering table: `sales_quotation`(+`_line`) — DRAFT→SENT→ACCEPTED/REJECTED/EXPIRED→CONVERTED with `subtotal/tax/total_minor` (integer tax = floor(subtotal × rate%)), and `sales_so`(sales order, +`_line`) tracking per-line `delivered_qty` and status DRAFT→CONFIRMED→PARTIALLY_DELIVERED→DELIVERED/INVOICED/CANCELLED. Customer = `crm_client` (composite FK), line product = `inventory_product` (optional, `SET NULL`). New `SalesService`/`SalesController`: quotation CRUD + status, **one-tx `convertToOrder`** (copies lines, marks the quote CONVERTED), order create/list/get, `fulfillOrder` (all lines delivered → DELIVERED) and `cancelOrder` (guards DELIVERED/INVOICED). Pure helpers `computeTotals`/`lineTotalMinor`/`nextSalesDocNo` + row mappers. Gated by `@RequiresFeature('sales')`; writes require SALES_REP/admin. Feature keys `sales.quotations`/`sales.orders`. Web: `Quotation`/`SalesOrder`/`SalesLine` types. | `pnpm -w typecheck/lint` 0; vitest **108/108** (sales suite 3: `lineTotalMinor`, `computeTotals` sum+integer-tax, zero-tax + fractional floor). Migration follows the idempotent tenant-table/RLS/FK helpers used across the build. |
| HR lookups + AI Insights + report builder (feature) | _(post-9.1 feature)_ | **Four additions.** (1) **Designations** — managed `hr_designation` list (CRUD) + the Add-Employee form now uses **Department + Designation dropdowns** (with a "Departments & designations" management dialog). (2) **Attendance times** — check-in/out time inputs in the daily grid (bulk upsert carries `check_in`/`check_out`; the day view returns them). (3) **AI Insights** (`modules/ai/ai-insights.*`, feature `ai`) — real, tested statistics computed tenant-locally (RLS): least-squares **sales forecast**, **lead scoring**, **inventory demand + days-to-stockout / reorder**, **attrition risk**, and **z-score anomaly** detection; `/ai/insights/*` endpoints + an AI dashboard page (the resilient Python ML bridge at `/api/ai/*` stays for heavy models). (4) **Report builder** (`modules/reporting/report-builder.*`, table `rpt_report_definition`) — preset reports + a **custom builder** over a server-side DATASET whitelist (the injection boundary: only whitelisted columns/tables emitted, all filter values parameterized); `/reports/builder/*` (datasets, presets, ad-hoc run, save/list/run/delete) + a builder UI. | `pnpm -w typecheck/lint` 0; vitest **105/105** (+ ai-insights 6, report-builder 6 incl. injection-rejection cases). Migration **idempotent**. Reviewer PASS — injection boundary sound, RLS on both tables, AI columns verified, no route collisions. |
| HR: profiles + attendance-driven payroll + policy module (feature) | _(post-9.1 feature)_ | **Three HR depth additions.** (1) **Complete employee profile** — `hr_employee` gains personal/contact/job columns (DOB, gender, CNIC, blood group, address, emergency contact, designation, employment type, reporting line, …) + child tables `hr_employee_education` and `hr_employee_experience`; new `HrProfileService`/`HrProfileController` (`getProfile`, `updateProfile`, education/experience CRUD); richer create form + a full employee profile page. (2) **Enterprise attendance** — `hr_attendance` gains work/overtime hours, late flag, notes + a per-day partial unique index for upsert; daily log + bulk-day upsert + day grid + monthly summary; **payroll now pro-rates basic pay by attendance** (`hr_payroll_run.working_days`, `hr_payslip.working_days`/`payable_days`; pure `attendancePayableDays`/`proratedBasicMinor` — present/paid-leave=1, half=0.5, absent=0, capped, full pay when untracked). (3) **Configurable policy module** — `hr_custom_field` (company-defined EAV fields) → `hr_policy` → `hr_policy_field_value`; new `HrPolicyService`/`HrPolicyController`; policy form renders the tenant's custom fields dynamically. Feature key `hr.policies`. | `pnpm -w typecheck/lint` 0; vitest **93/93** (HR suite 9: + `attendancePayableDays` weighting, `proratedBasicMinor` 4 branches). Migration **idempotent** (2nd run → "No migrations are pending"). Reviewer PASS — no route shadowing, no RLS gap, parameterized SQL. |
| HR: enterprise HCM suite (feature) | _(post-9.1 feature)_ | **Elevated HR into a full HCM suite** (raw SQL + `tenantTx`, like CRM/inventory). Migration `HrEnterprise` adds 11 tenant-scoped tables + `hr_doc_seq` numbering: **leave** (`hr_leave_type` entitlement → `hr_leave_balance` per employee/type/year → numbered `hr_leave_request` PENDING→APPROVED/REJECTED, approval consumes balance via `INSERT…ON CONFLICT`); **payroll** (`hr_salary_component` EARNING/DEDUCTION, FIXED or %-of-basic → one `hr_payroll_run` per month materialising a `hr_payslip`+`hr_payslip_line` per active salaried employee via the pure `computePayslip`); **performance** (`hr_performance_review` 1–5, `hr_goal` progress 0–100); **lifecycle** (`hr_employment_history` audit + `hr_document`). New `HrEnterpriseService`/`HrEnterpriseController`: leave types/balances/requests + decide, components + payroll run/approve/pay + payslips, reviews + goals, one-tx `recordLifecycleEvent` (updates employee + writes history), and HR reports (headcount by dept/status, payroll summary by component, leave summary). Events `hr.leave_approved.v1` + `hr.payroll_run_completed.v1` via the outbox. Feature keys `hr.leave`/`hr.payroll`/`hr.performance`/`hr.reports`. Web: HR sub-nav (Employees/Leave/Payroll/Performance/Reports) + 4 pages + 6 dialogs. | `pnpm -w typecheck/lint` 0; vitest **91/91** (HR suite 7: `inclusiveDays`, `computePayslip` earnings/deductions/percent + zero-skip). Migration **idempotent** (2nd run → "No migrations are pending"). Reviewer audit PASS on all four non-negotiables. |
| CRM: enterprise sales suite (feature) | _(post-9.1 feature)_ | **Elevated the minimal CRM into a full sales suite.** Migration `CrmEnterprise` enriches `crm_client` into proper **accounts** (numbered `account_no` via a new per-tenant `crm_doc_seq`, plus phone/email/address/owner/annual-revenue) and `crm_deal` into **opportunities** (`probability` + `CHECK 0..100`, `owner_id`, `source`, originating `lead_id`, `closed_at`/`lost_reason`); adds `crm_lead` (capture→qualify funnel NEW→CONTACTED→QUALIFIED→UNQUALIFIED→CONVERTED, HOT/WARM/COLD rating) and `crm_activity` (CALL/MEETING/EMAIL/TASK/NOTE timeline linked to account/contact/deal/lead, due + completion). New `CrmEnterpriseService`/`CrmEnterpriseController`: leads CRUD + status, **one-tx `convertLead`** (account[+reuse] → primary contact → optional opportunity → mark CONVERTED → `crm.lead_converted` outbox event), activity timeline + open-task feed + complete, and **sales reports** (weighted forecast, win/loss + win-rate, sales-by-owner leaderboard, lead funnel). `CrmService` deals now carry a probability-weighted forecast (`STAGE_PROBABILITY`), and the pipeline board shows it (zeroed for closed stages). New event contract `crm.lead_converted.v1` (shared). Web: CRM sub-nav (Pipeline/Leads/Accounts/Activities/Reports), 4 new pages + 5 dialogs (new account/lead/deal/activity, convert-lead), weighted-forecast stat on the board. Feature keys `crm.leads`/`crm.activities`/`crm.reports`. | `pnpm -w typecheck/lint` 0; vitest **88/88** (CRM suite 7: `weightedMinor`, weighted `buildPipeline` incl. closed-stage zeroing, `mapDealRow`/`mapLeadRow`, `STAGE_PROBABILITY` monotonic). Migration **idempotent** (2nd run → "No migrations are pending"). |
| Inventory: category hard-delete (fix) | _(post-9.1 fix)_ | **Category delete now reflects in the DB.** `removeCategory` previously soft-deleted (set `deleted_at`), so the row stayed in `inventory_category` (invisible to the app but still holding the unique sibling-name slot — a deleted name couldn't be reused). Categories are guarded reference data (delete already refuses while children/products remain) with nothing to audit, so the row is now **physically removed** (`DELETE`). Migration `InventoryCategoryHardDelete` purges any rows left tombstoned by the old behaviour (idempotent; `down` is a no-op). | `pnpm -w typecheck` 0; vitest 7/7 (inventory). New migration idempotent (2nd run → "No migrations are pending"). Verified on the running stack: create→delete→re-create same name now succeeds (was 400 "already exists"). |
| Inventory: category tree (feature) | _(post-9.1 feature)_ | **3-level product categories** (category → sub-category → sub-sub-category). Migration `InventoryCategories` adds `inventory_category` (RLS, composite self-FK `(tenant_id,parent_id)`→`(tenant_id,id)` `ON DELETE CASCADE`, `CHECK level BETWEEN 1 AND 3` + a parent/level invariant, case-insensitive sibling-name unique index with the NULL parent folded to a sentinel uuid) and `inventory_product.category_id` (nullable FK `ON DELETE SET NULL`); the legacy free-text `category` column stays for back-compat. New `InventoryCategoriesService` (list with child/product counts, nested tree, create — derives `level = parent.level + 1` and rejects depth > 3, rename, soft-delete that refuses while children/products remain) + pure helpers `buildCategoryTree`/`buildCategoryPath`. `InventoryService` products SELECT now left-joins up to 3 category levels so every product carries its top→leaf `categoryPath`; `createProduct` accepts `categoryId`; `PATCH /inventory/products/:id/category` re-classifies. Category CRUD + `GET /inventory/categories[/tree]` under `@RequiresFeature('inventory')`, writes gated to INVENTORY_MANAGER. Added `inventory.categories` feature key. Web: Categories tab + tree page (recursive node, add-sub, delete), create-category dialog, category picker in the new-product dialog, category path shown on the stock list. | `pnpm -w typecheck/lint` 0; vitest **84/84** (4 new: `buildCategoryPath` ordering/drops, `buildCategoryTree` 3-level nest + orphan-as-root, joined `mapProductRow`). Migration **idempotent** (2nd `migration:run` → "No migrations are pending"). |
| 9.4 CD + runbook + final acceptance | `ckpt/9.4-release-stable` | **PHASE 9 / BUILD CHAIN COMPLETE.** `.github/workflows/deploy-staging.yml` (on merge to main: build + push the 4 prod images to GHCR with layer cache; **canary deploy** to a staging host with a **health-gated automatic rollback**, guarded behind the `STAGING_HOST` repo var). `docs/runbook.md` (deploy, rolling restart w/ zero in-flight loss via the outbox, rollback, alert→action table, DLQ drain, graceful-degradation expectations, scaling). `infra/scripts/acceptance.sh` — full end-to-end acceptance against the running stack. CLAUDE.md updated with a Production & Operations section. | **Acceptance 14/14 against the live prod stack:** provision tenant → login → exercise HR/Finance/Inventory/CRM → fire **inventory.low_stock + finance.invoice_paid + crm.deal_closed** → all 3 written to the outbox → **relay published every one (none pending)** → consumers reacted (4 reactions) → **kill ML → AI stays 200 `degraded:true`, liveness 200** → ML restored. `pnpm -w test` 80 + `typecheck` clean; `deploy-staging.yml` actionlint exit 0. |
| 9.2 Migrations finalize + seed + backup | `ckpt/9.2-data-stable` | **PHASE 9 cont.** `infra/scripts/backup.sh` (pg_dump custom format, timestamped, container or `PGURL`, keeps GRANTs/strips ownership, optional S3/MinIO upload) + `restore.sh` (schema reset + `pg_restore`, confirm guard). `infra/scripts/seed-demo.sh` — idempotent demo seed: bootstraps a SUPER_ADMIN via SQL (argon2), provisions the **enterprise-plan** demo tenant via the API, then seeds **HR / Finance / Inventory / CRM** through the real endpoints (departments+employees; 4-level COA + cash/bank tags + currencies + vouchers + invoice; warehouse+products+movement; client+contact+deal). PgBouncer prod config (`pool_mode=transaction, max_client_conn=200`) confirmed in both compose files. `backups/` gitignored. Note: a large suite of enterprise-accounting features (GL, statements incl. cash flow, AR/AP+aging, cost centers, budgets, year-end close, recurring vouchers, multi-currency, bank-statement auto-match) was also built across post-9.1 feature work — see the finance rows below + git log. | **Empty DB → migrate → seed → app works:** fresh `seed_test` DB, 30 migrations applied, host API booted (`/health` 200), seed ran clean → tenants=1, users=2, hr_employee=2, finance_account=13, finance_invoice=1, currency=2, inventory_product=2, crm_deal=1; **2nd seed run = no-op (idempotent)**. **Backup→restore:** prod DB dumped + restored into a scratch DB with identical row counts (29 accounts / 14 txns). |
| Finance: full accounting (feature) | _(post-9.1 feature)_ | **General Ledger, statements & multi-level Chart of Accounts.** Migration `FinanceCoaHierarchy` adds `parent_id` (composite self-FK `(tenant_id,parent_id)`→`(tenant_id,id)`) + `is_group` to `finance_account` (idempotent; no RLS change). Service: hierarchical `listAccounts` (recursive-CTE tree + `level`), group rules (parent must be a group; postings only to leaf accounts → 422), `listTransactions`, and read models — **general ledger** (`GET /finance/ledger/:accountId`, opening/running/closing in the account's normal sign), **trial balance** (`GET /finance/trial-balance`), **balance sheet** (`GET /finance/statements/balance-sheet`, A = L + E + net income) and **income statement** (`GET /finance/statements/income`). Pure helpers `normalBalance`/`signedBalanceMinor`/`trialColumns` (integer minor units, ADR-007). Web: finance sub-nav + Chart-of-Accounts tree, journal list + balanced multi-line composer, general-ledger viewer, and a reports page (TB/BS/P&L). | `pnpm -w typecheck/lint` 0; vitest **73/73** (4 new accounting-semantics tests). **finance-accounting-e2e 14/14** (COA tree depth, group-post 422, parent-not-group 422, journal list, ledger closing, TB balanced, P&L net, BS balanced) — wired into `test:e2e`. Migration idempotent (2nd run no-op). Verified live on the prod stack: seeded COA + 6 entries → TB/BS balanced, bank ledger closing exact, 5 web pages 200. |
| 9.1 Production Docker builds | `ckpt/9.1-images-stable` | **PHASE 9 started.** Production container images for every app + a full-stack prod compose. Multi-stage Dockerfiles for **api/worker** (`node:22-bookworm-slim` builder → corepack pnpm 11.5.2 → frozen workspace install w/ a `--mount=type=cache` pnpm store → build `@metaxperts/shared`+`config`+app → `pnpm --filter=<app> --prod --legacy deploy` prunes a self-contained tree → **`gcr.io/distroless/nodejs22-debian12:nonroot`** runtime, uid 65532, exec-form `HEALTHCHECK` via `/nodejs/bin/node` since distroless has no shell). **web**: enabled Next `output:'standalone'` + `experimental.outputFileTracingRoot`=repo root, runtime copies `.next/standalone`+`static` only, non-root. **ml** reused its existing prod Dockerfile (uid 10001). Build context = repo ROOT for the Node apps (need the workspace); ml builds from its own dir. Root `/.dockerignore` (effective one) + per-app `.dockerignore`. `infra/docker-compose.prod.yml` runs the WHOLE system in containers (infra + api/worker/web/ml) wired by service name via an `x-app-env` anchor (DATABASE_URL→pgbouncer as app_user/RLS, owner URL for relay/ml, redis/rabbit/otel/ml hosts, `OTEL_ENABLED=true`); `infra/prometheus.prod.yml` scrapes `api:3000`. Background workers OFF until the schema lands (9.2). Secrets via `${VAR:-…change_me…}` demo defaults / `*_FILE`. **`--legacy` deploy** required (pnpm v10+ refuses non-injected workspace deploy); **`apps/ml/package.json` re-included** in `.dockerignore` for the frozen install. | `docker compose -f infra/docker-compose.prod.yml build` (4 images) + `up` → **all 12 services healthy** (api/worker/web/ml + 10 infra; minio/otel distroless = running-state). **Non-root proven:** api/worker/web uid **65532**, ml uid **10001** (exec `getuid()` + image `Config.User`). Smoke: api `/health`+`/health/ready` 200, web `/` 200, ml `/health` 200. `pnpm -w typecheck/lint` 0; vitest **69/69**. |
| 8.3 Security hardening | `ckpt/8.3-security-stable` | **PHASE 8 COMPLETE.** Production security posture. **Helmet** (HSTS, X-Frame-Options, X-Content-Type-Options nosniff, X-Powered-By removed; CSP off for a JSON API). **CORS locked** to `CORS_ORIGINS` (was reflect-any). **Payload limits** (`MAX_BODY_SIZE`, body-parser → 413; the exception filter now honors framework 4xx like PayloadTooLarge instead of masking them as 500). **Secrets via a manager:** the config loader resolves the Docker/k8s `*_FILE` convention (`JWT_ACCESS_SECRET_FILE=/run/secrets/…`) so prod loads secrets from a mounted store, not plaintext env. **Dependency + container scanning in CI** (`.github/workflows/security.yml`: `pnpm audit --prod --audit-level critical` + Trivy on the ML image + pip-audit) — **bumped Next.js 14.2.21→14.2.35 to clear the critical middleware auth-bypass (CVE-2025-29927)**; prod-critical audit now clean. **Route audit** (`route-audit.cjs`): enumerates every route and asserts each non-allowlisted one rejects unauthenticated requests with 401 — the public surface is exactly `/health*` + `/auth/*` + `/metrics` (+ `/internal/*`, service-token-guarded). | `pnpm -w typecheck/lint` 0; web prod build green (Next 14.2.35); `pnpm audit --prod --audit-level critical` exit 0. **security-e2e 12/12**: helmet headers, CORS allow/deny, **413 on oversize body**, allowlist reachable + business routes 401, **route audit — 69 routes, all 63 non-allowlisted require auth**. Full regression: 69 spec + **216 e2e (20 scripts)**. |
| 8.2 Metrics + dashboards | `ckpt/8.2-metrics-stable` | RED + USE visibility. `GET /metrics` (prom-client, public + un-throttled, raw exposition via `@Res()` so it isn't enveloped). RED: `http_request_duration_ms` histogram + `http_requests_total` (global `MetricsInterceptor`, keyed by matched route PATTERN to bound cardinality). USE gauges refreshed by a periodic collector (privileged owner pool so RLS doesn't zero them): `metaxperts_active_tenants`, `metaxperts_outbox_lag` (+`oldest_age_seconds`), `metaxperts_dlq_depth`, `metaxperts_breaker_state{breaker}` (via a `breakerRegistry` the `CircuitBreaker` ctor self-registers into), `metaxperts_queue_depth` (RabbitMQ mgmt API). Infra: Prometheus now scrapes the API (`host.docker.internal:3300` + host-gateway), alert rules (`prometheus-alerts.yml`: DLQNotEmpty / OutboxLagHigh / CircuitBreakerOpen / ApiDown), Grafana datasource + dashboard provisioned. Open-source only. | `pnpm -w typecheck/lint` 0; vitest 69/69. **metrics-e2e 11/11** (`test:metrics`, standalone): all 6 custom metrics exposed; **Prometheus scrapes the API (target up) + custom metric queryable**; **Grafana dashboard + datasource provisioned**; **DLQNotEmpty alert FIRES** on a forced `dlq_event`. Found+fixed: a running demo API with the relay on globally consumes the outbox → interferes with tests; run the demo relay-less during the build. |
| 8.1 Distributed tracing | `ckpt/8.1-tracing-stable` | **PHASE 8 begins.** One connected trace from a user action through the broker to the in-process consumers. `apps/api/src/tracing.ts` (imported FIRST in main.ts) starts the OpenTelemetry NodeSDK with auto-instrumentations (http/express, pg, ioredis, **amqplib**, undici) + OTLP/HTTP export to the infra otel-collector; flag-gated `OTEL_ENABLED`. **Broker propagation** is automatic (amqplib instrumentation injects/extracts `traceparent` in message headers). **Across the async outbox**: `OutboxService.write` captures the active `traceparent` into a new `outbox_event.trace_context` column, and the relay **restores that context** (`context.with`) before publishing — so the publish/consume spans join the originating request's trace instead of starting fresh. Logs are trace-correlated (`traceId`/`trace_id`/`span_id` on every line). Collector `debug` verbosity → `detailed`. Open-source only. | `pnpm -w typecheck/lint` 0; migration idempotent; vitest 69/69 (outbox/relay change clean). **tracing-e2e 5/5**: closing a deal → the collector receives that action's **trace id across 57 spans** spanning the HTTP request → relay publish → **amqplib `crm.deal_closed` broker hop** → consume (one connected trace); request logs are trace-correlated. Full regression: 69 spec + **204 e2e (19 scripts)**. |
| 7.5 Chaos suite | `ckpt/7.5-chaos-stable` | **PHASE 7 COMPLETE.** A repeatable failure/chaos suite that locks in the resilience behaviours, + `docs/resilience.md`. `chaos-e2e.sh` (run via `pnpm --filter @app/api test:chaos`; a separate, allowed-to-be-slower job, NOT in the main chain) faults EACH dependency in a contained way — it boots a dedicated API instance with that one dependency's URL pointed at a dead port, so the shared infra is never disturbed — and asserts **bounded** behaviour: **DB** latency → `statement_timeout` cancels a 5s query at ~0.5s, process up; **ML** down → AI route 200 degraded (no 5xx), process up; **broker** down → closing a deal still 200 and the event stays safely PENDING in the outbox (not lost), process up; **Redis** down → liveness 200 but readiness 503 (bounded gate → LB pulls the instance). `docs/resilience.md` documents each dependency's timeout/retry/breaker/bulkhead and failure behaviour + how to run the suite. | `pnpm -w typecheck/lint` 0. **chaos-e2e 10/10** (all four dependencies fault boundedly, no cascade). Main regression unchanged (69 spec + 199 e2e). |
| 7.4 Overload & shutdown | `ckpt/7.4-overload-stable` | Behave well under overload and during deploys. **Rate limiting** (`@nestjs/throttler`): a `TenantThrottlerGuard` keys per-TENANT for authenticated requests / per-IP for anonymous (tracker from RequestContext); over the limit → **429 + Retry-After** (sheds load instead of falling over). Health probes are `@SkipThrottle()`d. **Graceful shutdown / readiness gating**: `ReadinessService` flips to draining on `BeforeApplicationShutdown` (holds `SHUTDOWN_DRAIN_MS`), and `/health/ready` returns **503 while draining** so a load balancer pulls the instance before it stops accepting → a rolling restart loses zero in-flight work (`enableShutdownHooks` already drains HTTP + consumers). **Backpressure**: consumer prefetch is now env-tunable (`CONSUMER_PREFETCH`). Config-driven throttle/drain/prefetch. | `pnpm -w typecheck/lint` 0; **overload spec 3/3** (readiness ready→draining; indicator healthy→503). **overload-e2e 6/6**: 12/12 `/health` never throttled; under a tight limit the first 5 succeed then **429 with Retry-After** while `/health` stays 200; **SIGTERM drains and exits cleanly** (not force-killed). Default limit 300 leaves every existing suite green. Full regression: 69 spec + **199 e2e (18 scripts)**. |
| 7.3 Order-to-cash saga | `ckpt/7.3-saga-stable` | Multi-step, multi-module workflow that recovers from partial failure. **Orchestration-based saga**: `OrdersService` runs reserve-stock → create-invoice → pay, **each step in its own transaction** (so a later failure needs explicit compensation, not a single rollback); on failure it compensates the completed steps in reverse (void the invoice, release the reservation) and marks the order `COMPENSATED`. Steps are idempotent (re-entry reuses the existing reservation/invoice). State in `sales_order` (PENDING→RESERVED→INVOICED→PAID / COMPENSATED) + `inventory_reservation` (HELD→CONSUMED/RELEASED), both RLS. Terminal outcomes emitted to the outbox (`orders.order_completed.v1` / `orders.order_compensated.v1`; added `orders` to EVENT_DOMAINS). `POST /orders` (test-only `failAt` hook injects a step failure); commit decrements `on_hand` under its `CHECK (>=0)`. | `pnpm -w typecheck/lint` 0; migration idempotent; vitest 66/66 (EVENT_DOMAINS change clean). **orders-e2e 22/22**: happy path → reservation CONSUMED + invoice PAID + stock 100→90 + completed event; **fail@reserve** → nothing held/invoiced; **fail@invoice** → reservation RELEASED, no invoice; **fail@pay** → invoice VOID + reservation RELEASED; **insufficient stock** → compensated — **stock unchanged (90) across every failure, no orphaned reservation/invoice**. Full regression: 66 spec + **193 e2e (17 scripts)**. |
| 7.2 Idempotency keys | `ckpt/7.2-idempotency-stable` | Safe client retries on unsafe POSTs. RLS `idempotency_record` table (unique (tenant,key), request_hash, status_code, response_body jsonb, completed). `IdempotencyService`: the claim is an atomic `INSERT … ON CONFLICT DO NOTHING` (concurrent first-requests can't both proceed) → `new` / `replay` / `in_progress` / `mismatch`; `complete` stores the response; `abort` releases an uncompleted claim on handler error so retries work. Global `IdempotencyInterceptor` (skips when no `Idempotency-Key` / no tenant / safe method): captures and replays the response at its own level in the interceptor chain (correct regardless of envelope ordering), status from `@HttpCode`/method, fingerprint = sha256 of method+path+stable-sorted body. Duplicate → stored response replayed (`Idempotency-Replayed: true`) without re-running the handler; key reuse with a different body → 422; in-flight → 409. | `pnpm -w typecheck/lint` 0; migration idempotent. **idempotency spec 5/5** (new→replay, in_progress, mismatch, abort-releases, tenant-scoped). **idempotency-e2e 8/8**: same key+body twice → SAME invoice id, **deep-identical response**, `Idempotency-Replayed` header, **exactly one invoice persisted**; same key different body → 422; no key → normal 201. Full regression: 66 spec + **171 e2e (16 scripts)**. |
| 7.1 Resilience I/O | `ckpt/7.1-resilience-io-stable` | **PHASE 7 begins.** No unbounded waits anywhere; no single dependency can sink the process. Reusable `common/resilience/` kit: `withTimeout` (bounds the wait), `withRetry` (capped backoff+jitter, `shouldRetry`), `Bulkhead` (per-dependency concurrency cap + bounded queue → `BulkheadFullError`, isolated failure domains), and the shared `CircuitBreaker` (lifted from the 6.5 ML bridge; old path now re-exports it). Applied across every outbound call: **DB** — `TenantTransactionService` now issues `SET LOCAL statement_timeout / lock_timeout / idle_in_transaction_session_timeout` on every tenant transaction (config-driven, PgBouncer-pool-safe); **broker** — relay publish wrapped in timeout + breaker (down broker → relay leaves the row pending and retries, never blocks); **ML** — added a concurrency `Bulkhead` (outside the breaker, so overload ≠ dependency failure); Redis/SMTP already bounded (1.x/5.1), S3 unused. | `pnpm -w typecheck/lint` 0; **vitest 61/61** (resilience spec 9/9: timeout bounds a slow op, retry is capped, bulkhead caps concurrency + rejects-when-full, breaker+timeout compose to bound a slow/erroring dep with no cascade; the `SET LOCAL` timeouts didn't disturb any spec). **resilience-e2e 8/8**: a 5s query is **cancelled at ~0.5s by statement_timeout** (not hung), ML-down AI route returns **200 degraded** (no 5xx), and `/health` + `/health/ready` stay **200 through every fault**. Full regression: 61 spec + **163 e2e (15 scripts)**. |
| 6.5 API↔ML bridge | `ckpt/6.5-bridge-stable` | **PHASE 6 COMPLETE.** The resilient NestJS→ML client, built to fail safe (ADR-006). `CircuitBreaker` (CLOSED/OPEN/HALF_OPEN; injectable clock for deterministic tests). `MlService`: every call carries the signed `x-service-token` and is wrapped in a **timeout** (AbortController) → **retry+backoff** → **circuit breaker**; the public methods NEVER throw on ML failure — they return a typed `degraded` response (`source: 'live'|'cache'|'degraded'`), serving the **last-known forecast from Redis** (TTL 1h) on a miss. `AiController` (`/api/ai/*`, tenant always from the JWT principal): `POST /api/ai/forecast/:productId`, `/api/ai/anomalies/scan`, `/api/ai/invoice/extract` (multipart forwarded as `FormData`), `GET /api/ai/health` (breaker state). Bridge config (base URL/timeout/retries/threshold/cooldown/cache-TTL). | `pnpm -w typecheck/lint` 0. **ml-bridge spec 6/6** (`pnpm test -- ml-bridge`): breaker opens after threshold, **fails fast while open, half-opens after cooldown, closes on success / re-opens on a failed trial**; forecast/anomaly/invoice degrade instead of throwing; **last-known forecast served from cache when ML is down**; an open breaker short-circuits (no HTTP). **ml-bridge-e2e 11/11** (API booted against a DEAD ML endpoint): every AI route returns a graceful **200 `degraded`** (no 5xx cascade) + the breaker reports **OPEN**. Full regression: **52 spec + 155 e2e (14 scripts)**, zero failures. |
| 6.4 OCR + semantic search | `ckpt/6.4-ml-features-stable` | Invoice extraction + real semantic search backed by **pgvector**. **Infra:** swapped Postgres image `postgres:16-alpine`→`pgvector/pgvector:pg16` (same PG16 major → recreating the container kept the `pgdata` volume's data; reindexed for the musl→glibc collation change). Migration: `CREATE EXTENSION vector` + RLS `ml_embedding` table (`vector(384)`, unique (tenant,module,ref), btree scope index — **exact KNN**, deliberately not ivfflat: correct + better recall than ANN on small filtered per-tenant sets). **ML service:** `ocr.py` (pdfplumber for PDFs / pytesseract for images + regex parser → `{vendor,date,lineItems[],total,taxAmount}`, grand-total vs subtotal aware); `embedding.py` (fastembed ONNX `bge-small-en-v1.5`, 384-dim — the lightweight, torch-free, self-hostable stand-in for sentence-transformers, **baked into the image** so no runtime network). Endpoints (service-auth): `POST /ml/extract/invoice` (multipart), `POST /ml/search/index` (embed+upsert), `POST /ml/search` (top-k cosine, tenant+module scoped). Contracts mirrored py↔ts. Dockerfile installs tesseract + bakes the model. Open-source only. | `pnpm -w typecheck/lint` 0; migration idempotent; **vitest 46/46 + base ml-e2e 6/6 green against the pgvector DB** (image swap transparent). **pytest 16/16** (OCR parser + pdfplumber on a committed sample-invoice fixture). **ml-search-e2e 12/12** (docker, model baked): auth 401; index 3 docs → **semantic ranking correct** (financial query → audit doc, HR query → onboarding doc); **tenant isolation** (A never sees B's vector; B sees only its own); invoice extract → vendor/date/grand-total(4950, not the 4500 subtotal)/tax/2 line items. |
| 6.3 Anomaly detection | `ckpt/6.3-anomaly-stable` | Flag suspicious finance transactions. ML `anomaly.py` — pure `detect_anomalies(amounts)` scoring each with an **Isolation Forest** (scikit-learn) over transaction amounts; the `isAnomaly` decision uses a ROBUST modified z-score (median/MAD, |Mi|>3.5) with Tukey-fence fallback, which resists the masking effect that defeats a plain z-score / the contamination boundary on small samples; `reason` is a plain-language σ-from-mean. `db.fetch_transaction_amounts` (amount = Σ debits per transaction, date-range + tenant filtered). Endpoint (service-auth): `POST /ml/anomaly/transactions {tenantId,dateFrom?,dateTo?}` → `{count, anomalies, items:[{transactionId,amountMinor,occurredOn,score,isAnomaly,reason}]}`. Contract mirrored py↔ts. Open-source only (scikit-learn). | `pnpm -w typecheck/lint` 0; vitest 46/46 unaffected. **pytest 13/13** (Isolation-Forest path flags gross outliers + keeps the cluster normal; MAD/Tukey small-sample path; all-equal → none). **ml-anomaly-e2e 7/7** (docker, `--network host`): seed 15 tight + 2 gross-outlier transactions → **both 5,000,000 outliers flagged, anomalies a minority** → tenant-isolation (other tenant 0) → 401 without token. |
| 6.2 Forecasting | `ckpt/6.2-forecast-stable` | Demand forecasting that's **fast because it's precomputed**. `ml_demand_forecast` read-model table (RLS; `{dates,predicted,lower,upper}` jsonb, sentinel warehouse for "all"). ML service: `forecasting.py` — a PURE `forecast_demand(history,horizon)` fitting **ARIMA(1,1,1)** (statsmodels) with a robust naive fallback (recent mean ±1.96σ, demand clipped ≥0; lazy statsmodels import); `db.py` (psycopg, privileged + explicit tenant filter) reads OUT-movement daily demand (zero-filled) and reads/writes forecasts; `forecast_service.py` orchestrates fit-and-store + serve. Endpoints (service-auth): `POST /ml/forecast/refresh` (fit every product with OUT demand) and `POST /ml/forecast/demand {tenantId,productId,warehouseId?,horizon?}` — **serves the precomputed row** (`source:'cache'`), falling back to an **on-demand fit** only on a miss. Contract mirrored py↔ts (`packages/shared/src/ml.ts`). Dockerfile CMD now honors `ML_PORT`. Open-source only (statsmodels/pandas/numpy/psycopg). | `pnpm -w typecheck/lint` 0; migration idempotent; vitest 46/46 unaffected. **pytest 9/9** (pure forecaster: shape, horizon, non-negativity, band brackets prediction, ARIMA-vs-naive selection). **ml-forecast-e2e 11/11** (docker, `--network host`): seed 30 days OUT demand → refresh fits+stores → **cache serve correct shape, history=30, p95 0.037s < 200ms** → **on-demand fallback** for an un-precomputed product → 401 without token. base ml-e2e 6/6 still green. |
| 6.1 ML skeleton | `ckpt/6.1-ml-skeleton-stable` | **PHASE 6 begins (apps/ml, FastAPI).** Reproducible, Dockerized ML service with service auth. Hard-pinned `requirements.txt` (+ `requirements-dev.txt`); **slim multi-stage Dockerfile** (builder venv → non-root runtime, HEALTHCHECK). Structured JSON logging (structlog) + best-effort OpenTelemetry (instrument-on-OTLP-endpoint, never blocks startup). **ServiceAuth (`app/auth.py`)**: `/ml/ping` requires the API's `x-service-token` — an HS256 JWT verified with the shared `SERVICE_AUTH_SECRET`, audience `internal`, issuer `metaxperts-api` (exact mirror of the API's `ServiceTokenService`, ADR-006); `/health` stays public. Typed contract both sides: `app/contracts.py` (Pydantic) ↔ `packages/shared/src/ml.ts` (the bridge imports it in 6.5). Open-source deps only. | `pnpm -w typecheck` (python `compileall` + TS) 0; `pnpm -w lint` 0. **ml-e2e 6/6: `docker build` succeeds (deps install reproducibly) → `/health` 200 public → `/ml/ping` 401 (no token / bad signature / wrong issuer) → 200 with a valid host-signed token, caller=api.** pytest 5/5 (health public, ping token-gated incl. bad-sig/wrong-iss/valid). Run gate: `bash apps/ml/test/ml-e2e.sh`. |
| 5.3 Realtime | `ckpt/5.3-realtime-stable` | **PHASE 5 COMPLETE.** Authenticated, tenant-scoped realtime (Socket.IO). `RealtimeGateway` (`@WebSocketGateway`, CORS): the handshake must carry a valid access JWT (`auth.token` / `?token=` / Bearer header), verified with `JWT_ACCESS_SECRET` exactly as JwtAuthGuard does → the socket joins room `tenant:<tenantId>`; unauthed/tenant-less sockets are disconnected. `emitToTenant` addresses every emit to a tenant room, so cross-tenant leakage is impossible. `RealtimeBridge` (fed from domain events, **not** direct coupling): subscribes to `finance.invoice_paid` / `inventory.low_stock` / `crm.deal_closed` on a per-instance **exclusive, auto-deleted** queue and re-emits to the event's tenant room; best-effort (a broker outage degrades realtime without downing the gateway). Open-source deps only (socket.io, @nestjs/websockets/platform-socket.io). | `pnpm -w typecheck/lint/build` 0; socket.io handshake 200; gateway+bridge init on boot. realtime-e2e 5/5 (socket.io-client harness) — **unauthenticated socket rejected (never 'ready'); two authed sockets join their rooms; closing a deal in tenant A reaches A's socket as `crm.deal_closed.v1` but tenant B's socket receives NOTHING (isolation proven)**. Full regression: **46 spec + 144 e2e (13 scripts)**, zero failures. |
| 5.2 Reporting | `ckpt/5.2-reports-stable` | Cross-module reports served from **per-tenant read models** (not inline aggregation). Reporting migration: 4 RLS read-model tables (`rpt_finance_monthly` P&L buckets, `rpt_inventory_valuation`, `rpt_hr_headcount`, `rpt_crm_pipeline`). `ReportingService.recompute` replaces a tenant's rows inside its RLS context (DELETE + `INSERT…SELECT` from the operational tables, both scoped by `app.tenant_id`); `refreshForTenant`/`refreshCurrent`/`refreshAllTenants`. `ReportingRefreshScheduler` (flag `REPORTING_REFRESH_ENABLED`, no-overlap interval). Endpoints (each returns `{ raw, series, totals }`): `GET /reports/finance/profit-loss?from=&to=`, `/reports/inventory/valuation`, `/reports/hr/headcount`, `/reports/crm/sales-pipeline`; `POST /reports/refresh` (TENANT_ADMIN, on-demand). **Also hardened the 4.3 consumer**: `IdempotentConsumer` now wraps DLQ `park` in `safePark` so a poison message whose park itself fails (e.g. a non-uuid tenant from a stray/foreign publisher) is logged-and-dropped instead of crashing the consumer process (surfaced when 5.1's durable queue captured an `eventbus.spec` test publish). | `pnpm -w typecheck/lint/build` 0; migration idempotent. reporting spec 5/5 — **empty before refresh → populated after** (proves read-model serving), `{raw,series}` shape + correct aggregates (P&L 100k/40k/60k, valuation, headcount, pipeline), `[from,to]` filter, **tenant isolation** (A's refresh never leaks into B). reports-e2e 17/17 (data via real module APIs → refresh → all 4 reports correct, cross-tenant empty, **served < 1s**). Full regression: **46 spec + 139 e2e (12 scripts)**, zero failures. |
| 5.1 Notifications | `ckpt/5.1-notifications-stable` | **PHASE 5 begins.** Event-driven notifications (in-app + email). `notification` table (RLS, partial unique `(tenant,user,source_event_id)` for idempotency, unread index). `NotificationsService` (`createInTx` idempotent insert / `listUnread` / `markRead`). `NotificationsConsumer` (flag-gated `NOTIFICATIONS_ENABLED`, in-process per ADR-001) subscribes via the `IdempotentConsumer` to `crm.deal_closed` → creates an in-app notification for the deal's **assignee** (additive `assignedTo` on `CrmDealClosedV1`, emitted by CRM `updateStage`). Email side-channel: `MailerService` (Nodemailer, short timeouts) + `EmailQueueService` (BullMQ queue+worker, exponential-backoff retries, flag-gated `NOTIFICATIONS_EMAIL_ENABLED`); best-effort `enqueue` swallows errors so SMTP/Redis outages never block the durable in-app row. `GET /notifications` + `POST /notifications/:id/read` (auth+tenant scoped; recipient is always the JWT principal). Open-source deps only (nodemailer, bullmq). | `pnpm -w typecheck/lint/build` 0; migration idempotent (run twice → no-op). notifications spec 6/6 — deal_closed→one notification for the assignee, **idempotent under redelivery**, **in-app survives a failing email side-channel (graceful)**, unassigned→none, tenant-correct, listUnread/markRead + 404-on-other-user. **notifications-e2e 13/13: the FULL pipeline** — close a deal → outbox → relay → RabbitMQ → consumer → assignee's in-app notification, read+mark-read over HTTP, 401 unauth, 404 unknown, cross-tenant isolation. Full regression: 42 spec + **122 e2e (11 scripts)**, zero failures. |
| Web 2.0 Polish | `ckpt/web-2.0-polish` | Master UI/UX pass + fleshed-out module screens (ADR-008/009). **Design system:** elevated depth/glow/motion — refined 4-theme tokens each w/ a `--glow`, `boxShadow.glow`, utilities (`.glass`/`.elevated`/`.glow-primary`/`.hover-lift`/`.ambient`/`.shimmer`/`.text-gradient`), keyframes (fade-up/scale-in/glow-pulse/shimmer), button micro-interactions (`active:scale`/hover-glow). Owned primitives added: dialog/select/table/skeleton/separator/sonner. Reusable patterns: `PageHeader`, animated count-up `StatCard`, `EmptyState`. **Shell:** glass sticky topbar, glowing active-nav sidebar w/ ambient blob, ambient content column. **Screens:** HR employees (paginated table + search + status filter + create dialog), Finance invoices (filter + pagination + mark-paid→event + create dialog w/ live total), Inventory (3 stat cards + low-stock highlighting + stock movement dialog + create), CRM pipeline (stat cards + glassy hover-lift deal columns), dashboard (count-up stats + staggered module cards), premium glow login. `lib/api` `apiList` preserves `{data,meta}` pagination. | `pnpm -w typecheck/lint` 0; web prod build green (12 routes, all static-prerendered); API+web serve 200; **all dialog POST shapes verified live against the API** (employee/product/movement/invoice each 201, invoice total computed correctly 27,500.00 PKR); demo seeded (2 employees, 2 products incl. a low-stock one, 1 stock movement, 1 invoice) — screens render real API data across 4 runtime themes. |
| Web 1.0 Foundation | `ckpt/web-1.0-foundation` | Enterprise web foundation (ADR-008/009). `apps/web`: Tailwind + shadcn-style owned primitives + **4 runtime themes** (light/dark/midnight/emerald via CSS-variable design tokens) + theme switcher (next-themes). API client (unwraps `{data,meta}`, transparent 401-refresh) + auth context (login/refresh/me/logout); enterprise **login page**; route-guarded app shell with **feature-driven sidebar** (renders only the tenant's enabled modules from `GET /tenant/features`) + topbar (user/roles/theme). Pages: dashboard, **Settings→Features toggle UI** (`PATCH /tenant/features/:key`), CRM **pipeline board**, module placeholders. API: CORS enabled for the web origin. | `pnpm -w typecheck/lint/build/test` 0; web prod build (12 routes); all routes serve 200; login UI renders; 4 themes compiled into CSS; CORS header present; demo tenant (admin@acme.test) provisioned w/ 6 deals — feature nav + pipeline render real API data. |
| 4.4 Worker handlers | `ckpt/4.4-worker-stable` | **PHASE 4 COMPLETE.** Three idempotent reaction handlers (raw SQL): `inventory.low_stock`→`po_suggestion`, `finance.invoice_paid`→`finance_balance` (upsert), `crm.deal_closed`→`commission` (5%). Reactions effect-table migration (RLS). `ReactionsService` registers them via `IdempotentConsumer` (flag-gated `WORKER_REACTIONS_ENABLED`, in-process per ADR-001). `/worker/health/ready`. Load test harness `load-reactions.cjs`; **ADR-001 decision gate recorded.** | `pnpm -w typecheck/lint/build/test` 0; worker-handlers 4/4 (each idempotent under redelivery: 1 PO/balance-once/1 commission; tenant-correct). **Load test: 10k events all processed in 8.85s ≈ 1,130 events/sec end-to-end → DECISION: stay NestJS, no Go** (numbers in ADR-001). Full regression 109/109 across 10 suites. |
| 4.3 Consumers | `ckpt/4.3-consumers-stable` | Idempotent consumers + DLQ + backoff (ADR-004). `processed_event` (unique (consumer,event_id)) + `dlq_event` migration (RLS). `IdempotentConsumer.handleOnce` (insert processed_event + handler effect in ONE tenant tx → redelivery is a no-op). `register` builds a per-consumer RabbitMQ retry topology (main queue + retry queue with per-message TTL → exponential backoff+jitter → dead-letters back; after maxAttempts → DB DLQ; malformed/poison → DLQ immediately). `DlqService` (park/list/requeue) + `GET /admin/dlq` + `POST /admin/dlq/:id/requeue` (TENANT_ADMIN). | `pnpm -w typecheck/lint/build/test` 0; consumers integration 3/3 — **same event twice → effect applied ONCE**; throwing handler **retried w/ backoff → parked in DLQ (attempts=3)**; requeue republishes + removes. dlq admin e2e 5/5 (VIEWER 403, RLS-scoped list, requeue). Full regression 109/109 across 10 suites. |
| 4.2 EventBus | `ckpt/4.2-eventbus-stable` | RabbitMQ EventBus + versioned contracts. `packages/shared/events`: typed `*.v1` contracts (`inventory.low_stock.v1`/`finance.invoice_paid.v1`/`hr.employee_created.v1`/`crm.deal_closed.v1`) + `EventPayloads` map (mismatch fails typecheck) + `domainOf`. `EventBusService` (amqplib): topic exchange per domain, durable queues, resilient connect, persistent messages, typed `publish`/`subscribe`. `RabbitMqPublisher implements Publisher` → relay now publishes through the bus (swapped from LoggingPublisher). Finance/Inventory/CRM now emit the `.v1` types. | `pnpm -w typecheck/lint/build/test` 0; eventbus integration test 4/4 — each contract round-trips with its typed payload via RabbitMQ; **a mismatched payload fails typecheck (`@ts-expect-error`)**. Relay→RabbitMQ smoke (event published+marked). Full regression 104/104 across 9 suites. |
| 4.1 Outbox relay | `ckpt/4.1-outbox-stable` | **PHASE 4 begins.** `OutboxRelay` polling relay (ADR-004): `SELECT … WHERE published_at IS NULL ORDER BY occurred_at FOR UPDATE SKIP LOCKED LIMIT N`, publish each via a `Publisher` abstraction (default `LoggingPublisher`; RabbitMQ in 4.2), stamp `published_at` on success / `attempts++` on failure (per-row, one bad event doesn't block the batch). Privileged relay DataSource (owner, bypasses RLS — sees all tenants; only connected when enabled). `OutboxRelayScheduler` (interval, no overlap, `OUTBOX_RELAY_ENABLED`). | `pnpm -w typecheck/lint/build/test` 0; outbox integration test 3/3: **100 events published exactly once + marked**; failing publisher loses nothing (attempts++) then publishes on retry; **crash-safe — rows locked by a rolled-back relay are re-published, never double-marked** (SKIP LOCKED). Enabled-path runtime smoke verified. Full regression 104/104. |
| 3.4 CRM | `ckpt/3.4-crm-stable` | **PHASE 3 COMPLETE.** CRM: `crm_client`/`crm_contact`/`crm_deal` (RLS, composite FKs, deal value minor units, stage/status CHECKs). `CrmService`: clients/contacts/deals CRUD, `GET /crm/deals/pipeline` (grouped by stage, zero-filled, Money totals), `PATCH /crm/deals/:id/stage` → on first reaching CLOSED_WON writes `crm.deal_closed` to the outbox (same tx). `@RequiresFeature('crm')`, SALES_REP writes. | `pnpm -w typecheck/lint/build/test` 0 (crm unit 3). crm e2e 15/15 (pipeline grouping+totals, deal_closed→outbox once, **idempotent CLOSED_WON transition**, cross-tenant 404); full regression 104/104 across 9 suites. **Phase 3 END GATE: all four modules tenant-scoped + emitting domain events to the outbox.** |
| 3.3 Inventory | `ckpt/3.3-inventory-stable` | Inventory: `inventory_warehouse`/`inventory_product`/`inventory_stock_movement` (RLS, composite FKs, money minor units, `CHECK (on_hand >= 0)`). `InventoryService.createMovement` locks the product `FOR UPDATE`, records the movement, writes `inventory.low_stock` to the outbox **before** the `on_hand` UPDATE, so an oversell's CHECK failure rolls back the event with the movement. `GET /inventory/products/low-stock`. `@RequiresFeature('inventory')`, INVENTORY_MANAGER writes. | `pnpm -w typecheck/lint/build/test` 0 (inventory unit 3). inventory e2e 16/16 incl. **ATOMICITY: oversell OUT → 422, on_hand unchanged, NO outbox row, no movement row**; low-stock emit on threshold cross; low-stock query; cross-tenant 404. Full regression 89/89 across 8 suites. |
| 3.2 Finance | `ckpt/3.2-finance-stable` | Double-entry-safe Finance + transactional-outbox foundation. `outbox_event` table (RLS, partial pending index) + `OutboxService.write(manager,...)` (same-tx). Finance migration: chart of accounts (type CHECK), transactions + journal entries (debit/credit XOR + non-neg CHECKs, composite FKs), invoices (money as bigint minor units, status CHECK). `FinanceService`: `assertBalanced` (≥2 entries, debit XOR credit, **sum(debits)==sum(credits) on integers**, rejects floats), invoice totals, **pay → writes `finance.invoice_paid` to the outbox in the same transaction**. `@RequiresFeature('finance')`, FINANCE_MANAGER writes. | `pnpm -w typecheck/lint/build/test` 0 (finance unit 7: balanced/unbalanced/float/multi-line). finance e2e 14/14 (balanced 201, **unbalanced 422, float 400**, invoice Money totals, filter, pay→outbox pending, cross-tenant 404); full regression 73/73 across 7 suites. |
| 3.1 HR | `ckpt/3.1-hr-stable` | **PHASE 3 begins.** HR module: `hr_department`/`hr_position`/`hr_employee`/`hr_attendance` migration — BaseEntity + RLS on each, **composite FKs on (tenant_id, id)** (cross-tenant FK refs impossible), salary as integer minor units + currency (ADR-007), per-tenant unique employee code, indexes. `HrService` (raw parameterized SQL via TenantTransactionService) + pure helpers (pagination/filter/Money-map). `HrController` gated by `@RequiresFeature('hr')`; writes require HR_MANAGER/admin, reads any role. CRUD + `GET /hr/employees?department=&status=&search=` paginated `{data,meta}`. | `pnpm -w typecheck/lint/build/test` 0 (hr unit tests 4); hr e2e 17/17 (feature gate, VIEWER write 403 / HR_MANAGER 201, CRUD, list+filter+pagination, salary Money, cross-tenant 404, soft delete); full regression 59/59 across 6 suites. |
| 2.5 Features | `ckpt/2.5-features-stable` | Per-tenant feature entitlements (ADR-009) — the add/remove-features backbone. Static module registry (6 modules + sub-features) + plan templates (starter/business/enterprise). `tenant_feature_entitlement` table (RLS, unique tenant+key) migration. `FeatureService` (Redis-cached reads, upsert+audit toggle, `applyPlan`), `@RequiresFeature` + global `FeatureGuard`. `GET /tenant/features` (catalog w/ enabled flags for the web nav), `PATCH /tenant/features/:key` (TENANT_ADMIN, audited). Provisioning applies a plan in-transaction. Fixed a guard-ordering bug (AuthModule now precedes FeaturesModule so the tenant is set before FeatureGuard reads it). | `pnpm -w typecheck/lint/build/test` 0; features e2e 11/11 (plan applied, gated route 403 when disabled → 200 when enabled, catalog reflects, toggle audited, VIEWER can't toggle); full regression 42/42 across 5 suites. |
| 2.4 Audit+SvcAuth | `ckpt/2.4-security-stable` | **PHASE 2 COMPLETE.** Audit trail: `audit_log` (RLS, tenant-scoped) migration; `AuditService` (own-tx + same-tx variants); global `AuditInterceptor` auto-logs POST/PATCH/PUT/DELETE with redacted body, `@SkipAudit` opt-out; `PATCH /users/:id/active` records explicit before/after. Service-to-service auth: `ServiceTokenService` (short-lived signed token), `ServiceAuthGuard` + `@ServiceOnly`, `GET /internal/ping`. RequestContext gained `ip`. API/worker now bind IPv4 `0.0.0.0` (avoids IPv6-loopback port collisions). | `pnpm -w typecheck/lint/build/test` 0; audit+svc-auth e2e 7/7 (before/after audit correct, secrets REDACTED, internal endpoint 401 without/with-bad/with-user token, 200 with valid service token); full regression 31/31 across 4 suites. |
| 2.3 Tenants | `ckpt/2.3-tenant-stable` | Tenant provisioning + per-request RLS scoping. `Tenant` entity + Tenants migration (platform registry, no RLS). `TenantsService.provision()` → creates tenant + first TENANT_ADMIN (admin written in the new tenant's RLS context; all-or-nothing). `POST /tenants` (SUPER_ADMIN-only), `GET /tenants/:id`. `GET /users/:id` tenant-scoped via TenantTransactionService. JwtAuthGuard rejects tenant-less tokens and seeds RequestContext so services run DB work with the tenant's `app.tenant_id` GUC. | `pnpm -w typecheck/lint/build/test` 0; tenant-isolation e2e 7/7: provisioning SUPER_ADMIN-only (403 otherwise), tenant created via API + its admin can log in, **acme admin GET globex user → 404 (RLS), own user → 200**. auth 8/8 + rbac 9/9 regression green. |
| 2.2 RBAC | `ckpt/2.2-rbac-stable` | Users + RBAC. `Role` enum (SUPER_ADMIN/TENANT_ADMIN/HR/FINANCE/INVENTORY/SALES_REP/VIEWER) + role→permission map (`domain:resource:action`, `*` wildcard). UsersRbac migration adds `roles text[]`/`employee_id` and extends `auth_lookup_user` to return roles → access JWT carries roles. Global guards (APP_GUARD order: `JwtAuthGuard` → `RolesGuard` → `PermissionsGuard`); `@Public()`/`@Roles()`/`@Permissions()`/`@CurrentUser()`; `/health*`+`/auth/*` public allowlist. `/me`, `/admin/ping`, `/admin/perm-ping`. Refresh now reloads roles+is_active from DB (role change/deactivation takes effect next refresh). | `pnpm -w typecheck/lint/build/test` 0; auth e2e 8/8 (regression); rbac e2e 9/9: public route open, unauth→401, bad token→401, VIEWER write→403, TENANT_ADMIN→200, permission guard 403/200, wildcard→200. |
| 2.1 Auth | `ckpt/2.1-auth-stable` | PHASE 2 start. `User` entity (tenant-scoped, RLS) + Users migration with a globally-unique email and a narrow `SECURITY DEFINER` `auth_lookup_user()` for the pre-tenant-context login lookup (granted only to app_user). Auth: argon2id password hashing, `@nestjs/jwt` 15m access tokens, opaque **rotating refresh tokens** in Redis with **family-based reuse detection** (replay → revoke family). `POST /auth/login|refresh|logout`, `@Public()` decorator. Open-source deps only (@nestjs/jwt, argon2). | `pnpm -w typecheck/lint/build/test` 0; auth e2e (8/8): login 200, bad-password 401, refresh rotates, **reused old refresh 401 + family revoked**, logout 204, refresh-after-logout 401. Login proves the SECURITY DEFINER lookup works under the RLS-enforced role. |
| 1.4 Tenancy | `ckpt/1.4-tenancy-stable` | The keystone: tenant isolation at the DATABASE. Non-superuser `app_user` role (init script + PgBouncer now connects as it, so app traffic is RLS-subject; migrations stay direct as owner). `BaseEntity` (uuid/tenant_id/audit/soft-delete), `TenantContext`, `TenantScopedRepository`, `TenantTransactionService` (`set_config('app.tenant_id', …, true)` transaction-local — correct under PgBouncer pooling), reusable `enableTenantRlsSql` (ENABLE+FORCE RLS + USING/WITH CHECK policy), `TenancyKernel` migration + `tenant_probe` canary. | `pnpm -w typecheck/lint/build/test` all 0; RLS integration test (5/5): tenant A↔B isolation, **zero rows without tenant context**, cross-tenant write rejected by WITH CHECK, owner sees both rows (proves RLS not emptiness); migration idempotent; app boots as `app_user` via PgBouncer with readiness 200. **PHASE 1 COMPLETE.** |
| 1.3 Kernel | `ckpt/1.3-kernel-stable` | NestJS API kernel (no business logic): `ConfigModule` (@nestjs/config + zod validate + `.env` load, fail-fast), `DatabaseModule` (TypeORM/pg through PgBouncer, `synchronize:false`, `autoLoadEntities`), `data-source.ts` + `migration:*` CLI (direct-to-Postgres), global `ValidationPipe` (whitelist+forbidNonWhitelisted), RFC 7807 `AllExceptionsFilter`, `{data,meta}` `TransformInterceptor`, pino structured logging w/ requestId, `RequestContext` (AsyncLocalStorage), Redis client + Terminus `/health` + `/health/ready`, graceful shutdown. ADR-008/009 added. | `pnpm -w build/typecheck/lint` 0/0/0; `/health` 200 `{data:{status:'ok'}}`; `/health/ready` 200 (db+redis up) / **503** (redis down, problem+json); unknown route → RFC 7807 w/ traceId; **bad env → boot refused** (LOG_LEVEL validation); SIGTERM → graceful exit + port released; `migration:run` idempotent (creates table, 2nd run no-op). |
| 1.2 Infra | `ckpt/1.2-infra-stable` | `infra/docker-compose.yml` — postgres:16, pgbouncer (transaction pool), redis:7, rabbitmq:3-mgmt, minio, otel-collector (0.154.0), prometheus, grafana on one `erp` network with named volumes + healthchecks; `infra/otel-collector-config.yaml` + `infra/prometheus.yml`; `infra:up/down/logs/ps/reset` scripts. API env wired to PgBouncer (:6432) + Redis + RabbitMQ. **Deviations:** host :3000 occupied by a pre-existing service → API verified on :3300 and infra UIs mapped to free host ports (grafana :3003, minio :9100/:9101, pg direct :55432, redis :6380); otel image bumped 0.116.0→0.154.0 (0.116.0 layer wouldn't exec). | `docker compose ps`: 6 services healthy, minio+otel running (distroless — host-probed live); `psql` THROUGH PgBouncer returns PostgreSQL 16.14 (db reachable); API `/health` → 200 `{data:{status:'ok'}}`; rabbitmq/prometheus/grafana/minio all answer 200. |
| 1.1 Skeletons | `ckpt/1.1-skeleton-stable` | Compiling skeletons for all four apps + shared contracts: `apps/api` (NestJS, `GET /health` → `{data:{status:'ok'}}`), `apps/worker` (NestJS, `GET /worker/health`), `apps/web` (Next.js 14 dashboard shell), `apps/ml` (FastAPI `/health`); `packages/shared` (success/problem+json/Money/pagination/base-event contracts), `packages/config` (zod env schema consumed by api+worker). Base tsconfig → CommonJS/node. | `pnpm -w build` (all 6 projects compile, incl. Next prod build + type validation), `pnpm -w typecheck`, `pnpm -w lint` all exit 0; api boots and `/health` returns the required envelope. |
| 0.1 Genesis | `ckpt/0.1-genesis-stable` | pnpm monorepo scaffold: workspace layout, root TS/ESLint/Prettier/Vitest tooling, `CLAUDE.md` constitution, ADR-001..007, `.claude` hooks (secret-scan + typecheck) and subagents (explorer/reviewer/test-writer), `.env.example`. No business code. | `pnpm -w typecheck`/`lint`/`test` pass on the empty workspace; `CLAUDE.md` present; exactly 7 ADRs in `docs/adr`; `.claude/settings.json` hooks present. |
